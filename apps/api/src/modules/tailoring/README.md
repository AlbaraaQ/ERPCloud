# Tailoring pack (Phase 22)

Feature flag: `pack.tailoring`. `customer_measurements` stores versioned per-party measurement cards; latest lookup supports party and invoice context display. The legacy `AtiveCustMeasur` behavior is represented by active/current rows instead of overwriting history.

# خريطة العمل القادمة — من أين تبدأ؟

> تاريخ الكتابة: 2026-09-17 · الفرع: `arena/01a0889e-cloud-saas-erp` · آخر دفعة: `a5c998d`
> (المرحلة 11 — الجزء السادس: 📱 واتساب).
>
> **تحديث 2026-09-17 (جلسة P-C1):** لوحة المنصة — الجزء الأول «الأساس والقشرة وترميم
> الصلاحيات» مُنجَز على الفرع `arena/01a0acbb-cloud-saas-erp`؛ الأرقام في §5 حُدِّثت
> بعد التنفيذ، والوثيقة [`../PLATFORM_CONSOLE_P_C1_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C1_IMPLEMENTATION_REPORT.md).
>
> **تحديث 2026-09-17 (جلسة P-C2):** والجزء الثاني «العملاء في العمق» مُنجَز على الفرع
> نفسه: بطاقة العميل `/tenants/[id]` بثمانية تبويبات و15 مسار `/platform/tenants/:id/*`،
> وترحيل `0067_tenant_card.sql`. الوثيقة
> [`../PLATFORM_CONSOLE_P_C2_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C2_IMPLEMENTATION_REPORT.md).
>
> **تحديث 2026-09-17 (جلسة P-C4):** والجزء الرابع «الباقات والتراخيص والفوترة» مُنجَز على
> الفرع نفسه: ترحيل `0068_platform_billing.sql` (ستة جداول + دورة حياة على `tenant_subscriptions`)
> و21 مساراً وستّ شاشات — `/plans` بجدول حقوق، `/subscriptions` بدورة حياة كاملة، و**ثلاث شاشات
> جديدة**: `/invoices` (فواتير ضريبية + إشعارات دائن بسلسلة `PCN-`) و`/dunning` (سلّم المتابعة)
> و`/revenue` (MRR · ARR · المتأخّر)، مع ورقة A4 مكتفية بذاتها برمز ZATCA وتفقيط. الدفع يدوي
> أولاً، والضريبة والمهلة وهوية البائع من `platform_settings`. الوثيقة
> [`../PLATFORM_CONSOLE_P_C4_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C4_IMPLEMENTATION_REPORT.md).
>
>
> **تحديث 2026-09-17 (جلسة P-C5):** والجزء الخامس «الاستخدام والحصص» مُنجَز على الفرع نفسه:
> محرّك واحد يقيس ثمانية مقاييس ويرفض عند الحدّ (`409 USAGE_LIMIT_REACHED`) وينبّه عند 80٪،
> وترحيل `0069_usage_metering.sql` (جدول `usage_counters`)، وثلاثة أسطح — تاب «الاستخدام» في
> بطاقة العميل (إسقاط على المحرّك لا حساب ثانٍ) و`/usage` في اللوحة (شبكة + رسوم + تصدير CSV)
> و`/settings/usage` في staff. **الحدود الافتراضية تُبلَّغ ولا تُطبَّق** — قرارٌ مصرَّح به في §3
> من التقرير. الوثيقة
> [`../PLATFORM_CONSOLE_P_C5_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C5_IMPLEMENTATION_REPORT.md).
>
> **تحديث 2026-09-17 (جلستان P-C6 وP-C7):** الجزء السادس «خدمة البريد» والجزء السابع
> «الإعلانات والإشعارات» مُنجَزان على الفرع نفسه: خدمة بريد بعشرة أوجه (قوالب 17 حدثاً ×
> لغتين · سجلّ · حجر · حصّتان · طابور · تراجع) وشاشتان (`/email` و`/settings/email`)، ثم
> الإعلانات: `0071_announcements.sql` · خمسة مسارات `/platform/announcements` · شاشة
> `/announcements` باللوحة · **مركز إشعارات في staff** (`/notifications` + جرس) يستهلك
> `/notifications` القائم بلا نقاط نهاية جديدة. الوثيقتان
> [`../PLATFORM_CONSOLE_P_C6_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C6_IMPLEMENTATION_REPORT.md) و
> [`../PLATFORM_CONSOLE_P_C7_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C7_IMPLEMENTATION_REPORT.md).
>
> **تحديث 2026-09-17 (جلسة P-C8):** والجزء التاسع «مكتب الدعم والدخول المؤقّت» مُنجَز على
> الفرع نفسه: ترحيل `0072_support_desk.sql` (**جداول ثلاثة**: `support_tickets` ·
> `ticket_messages` · `support_sessions`) وتسعة مسارات `/platform/*` — التذاكر بمهلةٍ محسوبة
> من الأولوية وبملاحظاتٍ داخلية لا تُرسل، و`break-glass` بمدّةٍ سقفها 60 دقيقة وسببٍ مكتوب
> وادّعاء `imp` في الرمز، وثلاث شاشات (`/tickets` · `/tickets/[id]` · `/impersonation`) مع
> **لافتة حمراء في staff** تُعلن الجلسة على كل شاشة. والحدود مفروضةٌ في الـAPI لا في الواجهة:
> لا `DELETE` ولا مسّ بـ`/auth/` خلال الجلسة، والإنهاء يُسقط الرمز في الطلب التالي. الوثيقة
> [`../PLATFORM_CONSOLE_P_C8_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C8_IMPLEMENTATION_REPORT.md).
>
> **تحديث 2026-09-17 (جلسة P-C9):** والجزء العاشر «**العمليات**» مُنجَز على الفرع نفسه: ثمانية
> مسارات `/platform/*` (الطابور عبر كل العملاء بنبض العامل وإعادة/إلغاء بسببٍ مكتوب · صحةٌ
> بستّة مجسّات و`p95` ولافتة حادثة · مدير ملفات بفحصٍ وحجرٍ وإبقاء ميتاداتا) ورمزٌ جديد واحد
> `console.jobs.manage` يفصل **الفعل عن القراءة** (المدقّق يقرأ ولا يشغّل)، وترحيل
> `0073_jobs_manage_permission.sql` بلا جدول ولا عمود كما قالت الخطة («ترحيل: —»). و**حكم فحص
> الملفات يُقرأ من مسار التدقيق** الذي كتبه `finalize`، و«لم يُفحص» يبقى مختلفاً عن «لم يُفحص
> فعلياً»؛ وأُضيف إلى `/audit` **عارض فرق** يعرض `before`/`after` اللذين كانا مخفيَّين منذ P-C1.
> الوثيقة [`../PLATFORM_CONSOLE_P_C9_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C9_IMPLEMENTATION_REPORT.md).
>
> **تحديث 2026-09-17 (جلسة P-C10):** والجزء الحادي عشر «**البيانات والاسترجاع**» مُنجَز على
> الفرع نفسه: نسخةٌ **تُغادر القاعدة فعلاً** إلى ملفٍ مشفّرٍ بـAES-256-GCM في S3/MinIO أو في
> بديلٍ مُعلَن على القرص، بحجمٍ وبصمةٍ **مقيسَين من الملف لا من الطلب**، و`verify` يعيد قراءة
> الملف ويمسحه سطراً سطراً ويقارن جداوله بكتالوج القاعدة الحيّ (استعادة تجريبية تقارن ولا تكتب)،
> وتنزيلٌ **برابط موقّع قصير العمر**. وسياسة احتفاظٍ بنوافذ تُضبط وتُنفَّذ فعلاً (حذف بايتات
> النسخ المنتهية · مفاتيح `idempotency` · مهامّ الطابور · الملفات اليتيمة) **وسجلّ التدقيق لا
> يُمحى أبداً**. وحقّ نسيانٍ يُنفَّذ على الهوية الحيّة: يُخفي 9 حقول ويُسحب العضويات ويُبطل
> الجلسات **ويقول كم صفّ تدقيقٍ بقي**، ولا يُنفَّذ إلا بتأكيدٍ ببريد صاحب البيانات نفسه.
> ثلاث شاشات/صفحتين: `/backups` و`/data-requests`، ورمزٌ جديد واحد `console.backups.manage`،
> وترحيل `0074_platform_backups.sql` (‏`backup_jobs` · `backup_artifacts` · `data_requests`).
> الوثيقة [`../PLATFORM_CONSOLE_P_C10_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C10_IMPLEMENTATION_REPORT.md).
>
> **تحديث 2026-09-17 (جلسة P-C11):** والجزء الثاني عشر «**بوابة المطوّر**» مُنجَز على الفرع
> نفسه: **مفاتيح API لكل مستأجر** (`erp_live_…` بنطاقاتٍ صريحة: النصّ يُعرض **مرّة واحدة**،
> والمحفوظ بادئةٌ وبصمة، مع تدويرٍ يُبطل القديم في اللحظة نفسها وإبطالٍ بسببه يبقى في السجلّ)،
> و**ويب هوك خارجٌ بعقدٍ موقّع** (`x-erp-signature: t=…,v1=…` على `<t>.<body>` بنافذة 300 ثانية)
> بخمسة مُنتِجين من مساراتٍ حقيقية (فواتير · وردية · زاتكا · مخزون · اشتراكات)، وسجلّ تسليمٍ
> **يقيس** (رمز الاستجابة · المحاولات · الزمن · الخطأ) مع إعادة إرسالٍ بعنوانها، و**مستكشف
> OpenAPI** يقرأ الوثيقة التي ينشرها الخادم نفسه. و**سطحٌ ثالث** `/integration/v1/*` يُصادَق
> عليه بالمفتاح وحده، ورمزان جديدان `console.apikeys.manage` و`console.webhooks.manage`،
> وترحيل `0075_developer_platform.sql` (‏`api_keys` · `webhook_endpoints` · `webhook_deliveries`
> · `api_key_uses`). والمستأجر التجريبي (**sandbox**) مؤجَّلٌ صراحةً — نطاقٌ قائم بذاته.
> الوثيقة [`../PLATFORM_CONSOLE_P_C11_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C11_IMPLEMENTATION_REPORT.md).

> **تحديث 2026-09-17 (جلسة P-C12):** والجزء الأخير «**التحليلات**» مُنجَز على الفرع نفسه — وبإتمامه
> **اكتملت أجزاء الخطة الاثنا عشر كلها**. **لا رقم مُخترع**: `mrr`/`arr`/التحصيل من لوحة الإيراد
> (P-C4) حرفاً بحرف (يقيسه السبيك)، والاستخدام وحدوده من شبكة P-C5 نفسها مع `limitSource` و
> `enforced` — فلا يدّعي التقرير سقفاً غير مُطبَّق. **التسرّب مرّتين** (شعاراتٌ ومال) ولكلٍّ مقامُه،
> و**النسبة بلا مقام `null` لا `٠٪`**. **قمعٌ بالمهل**: سجّل → فُعِّل → رحّل أوّل فاتورة → أوّل مستند
> زاتكا (وسيط الأيام والمئين ٩٠)، والخطوتان الأخيرتان تُقاسان بفعل العميل لا بفاتورتنا عليه.
> **أفواجٌ بخليّةٍ ذات رقمين**: متعاقدٌ ومستخدِم. و**ستة تنبيهات** لها مصادرُ في القاعدة ووجهاتٌ في
> اللوحة ولا يُعرض منها ما عددُه صفر. و**ملف CSV** بأربعة عشر عموداً تُطابَق حرفاً بحرف مع العقد.
> رمزٌ جديد واحد `console.analytics.view` (قراءةٌ خالصة) وترحيل `0076_analytics_permission.sql`
> **بلا جدول**. والمؤجَّل صراحةً: **التقرير الأسبوعي بالبريد** (كل ما يُحسب نُفِّذ، وبقي التسليم
> الدوري) وأفواج الإيراد وتصدير Excel/PDF.
> الوثيقة [`../PLATFORM_CONSOLE_P_C12_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C12_IMPLEMENTATION_REPORT.md).

> **تحديث 2026-09-18 (جلسة التقرير الأسبوعي — المكمّل الثاني لـP-C12):** أُنجز **التسليم الدوري**
> الذي أُجّل صراحةً في P-C12: قالبٌ من P-C6 (حدث `report.weekly` بنطاق `platform`، فلا يُحتسب على
> حصّة عميل) ومجدولٌ في العامل (نبضةٌ كل دقيقة على طابور «الصيانة» + **لحاق إقلاع** يمنع أن يُسقط
> انقطاعٌ تقريرَ أسبوع) و**أربعة مفاتيح إعداد** (`report.weekly_{enabled,recipients,day,hour}`) —
> فالمشغّل يضبط المستلم واليوم والساعة من شاشة الإعدادات بلا نشر. و**الحساب لا يُعاد**: الأرقام من
> `PlatformAnalyticsService.overview()` نفسها. والقرارات الأربعة (المستلم · الموعد · الفشل · ومنع
> التكرار من سجلّ البريد لا من عمود حالة) مكتوبةٌ في التقرير، مع مسارَي
> `GET /platform/reports/weekly` و`POST …/run` وبطاقةٍ في `/analytics`.
> الوثيقة [`../WEEKLY_REPORT_IMPLEMENTATION_REPORT.md`](../WEEKLY_REPORT_IMPLEMENTATION_REPORT.md).

> **تحديث 2026-09-18 (جلسة P-M1+P-M2+P-M5):** وفي خطة الموقع التسويقي
> ([`MARKETING_SITE_PLAN.md`](MARKETING_SITE_PLAN.md)) أُنجزت ثلاثة أجزاء على الفرع نفسه:
> **P-M1** (القشرة ونظام التصميم وSEO بنيوي ولغتان بـ`/en` و`hreflang`) · **P-M2** (الرئيسية
> والوحدات الستّ عشرة وقسم زاتكا والأسئلة الشائعة) · **P-M5** (نظام إدارة المحتوى كاملاً:
> خمسة جداول وتسعة أنواع محتوى وأحد عشر نوع كتلة وأربعة عشر مساراً في اللوحة ورمزا صلاحية
> جديدان وشاشة `/content` بأربعة تبويبات ومحرّك نشرٍ مجدول). وفوق الخطة: **404 بحالة 404
> فعلاً** عبر الوسيط (Next لا يُصدرها من صفحةٍ ديناميكية) · و**`kind` في `/public/posts` صار
> معاملاً** فلم تعد `/cases` فارغة أبداً · والتحقّق الحيّ **`verify-content.mjs` 62/62** و
> **`verify-marketing-site.mjs` 37/37**. الوثيقة
> [`../MARKETING_SITE_CMS_P_M1_M2_M5_IMPLEMENTATION_REPORT.md`](../MARKETING_SITE_CMS_P_M1_M2_M5_IMPLEMENTATION_REPORT.md).

> **تحديث 2026-09-18 (جلسة P-M3):** والجزء الثالث من خطة الموقع التسويقي — **الباقات والأسعار** —
> مُنجَزٌ على الفرع نفسه: `GET /public/plans` يقرأ **النشطة وحدها** بحقوقها **بلغتين** (والأسماء من سجلّ
> المنتج: `tenantFlagLabels` صار يحمل `labelEn`)، و`meta.vatRatePercent` من `billing.tax_rate` فلا رقمَ
> ضريبةٍ مكتوبٌ في الواجهة، و`/pricing` أُعيد بناؤها: مبدّل الدورة **رابطان** (`/pricing?interval=year`)
> لا حالةً في المتصفح، وجدول مقارنةٍ من الحقوق المعلنة فعلاً، وصفُّ «بوابات الدفع» قدرةٌ في المنتج
> بمصدرها لا مفتاحُ حقٍّ مُخترع، وثمانية أسئلة تسعير أجوبتها من قواعد المستودع. و**البذرة صارت تحمل
> حقوق الباقات الثلاث** (32 حقًّا) — تعديلها من اللوحة يظهر في الموقع فوراً. والتحقّق الحيّ
> **`verify-pricing.mjs` 53/53**. الوثيقة
> [`../MARKETING_PRICING_P_M3_IMPLEMENTATION_REPORT.md`](../MARKETING_PRICING_P_M3_IMPLEMENTATION_REPORT.md).

> **تحديث 2026-09-19 (جلسة P-M6):** والجزء السادس — **التقاط العملاء المتوقّعين وإدارتهم** —
> مُنجَزٌ على الفرع نفسه: `/contact` و`/demo` صارا استمارتَين حقيقيتين تكتبان في
> `POST /public/leads` بمصيدةٍ صامتة (`website` مخفيٌّ بـCSS — والردّ 202 بلا صفّ) ومحدّدِ معدّل
> على دلو `public-form`، **و`/demo` مسارٌ جديد** موسومٌ `demo` في الطابور (كان زرّاً يشير إلى
> `/contact`)، ونشرةٌ بريدية في التذييل **بلغتين** (التذييل يظهر في `/` و`/en`) بتأكيدٍ مزدوج
> (رابطُ رمزٍ ٤٨ حرفاً مخزونٌ sha256 وحده ولا يُعاد في أي استجابة، والتأكيد idempotent).
> وفي اللوحة شاشةُ **`/leads`** بتبويبين: الطابور (مرشّحات الحالة/المصدر/غير المُسنَد/طلباتي ·
> بحثٌ بالمرجع · عدّاداتٌ مع كل صفحة · بطاقةُ الطلب بالانتقالات وسببها والإسناد والملاحظات
> والأثر) والنشرة. **والقرار الحاكم: العميل المتوقَّع ليس مستأجراً** حتى تحويلٍ صريح يُنشئ
> المنشأة والمدير والفرع ودليل الحسابات وترخيصاً `trialing` مدّته من `billing.trial_days`
> (وكلمة المرور المؤقتة تُعاد **مرّةً واحدة**)، والحالات النهائية `won`/`rejected` لا تعود.
> **وخطأٌ حقيقي كشفه السكربت الحيّ**: `defaultPlanId` كان يسأل `is_active`/`deleted_at` في
> `billing_plans` (الصواب `active = true`) ⇒ **كل تحويل كان 500**، واختبارُ الوحدة مرّ لأن
> الاختبار يمرّر `planId` صراحةً. الدليل: `public-leads.spec.ts` **16** ·
> `leads.spec.ts` (contracts) **9** · `apps/marketing/tests/leads.spec.ts` **10** ·
> **`verify-leads.mjs` 58/58 في 6 أقسام**. الوثيقة
> [`../MARKETING_LEADS_P_M6_IMPLEMENTATION_REPORT.md`](../MARKETING_LEADS_P_M6_IMPLEMENTATION_REPORT.md).

> **تحديث 2026-09-19 (جلسة P-M7):** والجزء السابع — **الحملات البريدية وإلغاء الاشتراك** —
> مُنجَزٌ على الفرع نفسه: ثلاثُ جداول (`email_campaigns` · `campaign_messages` · `campaign_events`)
> تجعل التقرير **صفوفاً تُقرأ** لا عدّاداً في الذاكرة، ورمزُ كل رسالة **سرٌّ عشوائي مُجزَّأ**
> (sha256) يحرس الفتح والنقر والإلغاء، و**الوجهة تُقرأ من صفّ الرسالة** فلا تحويلَ مفتوحاً،
> و`List-Unsubscribe`/`List-Unsubscribe-Post` تخرجان مع كل رسالة **وتُحفظان** معها.
> وشاشة **`/campaigns`** في اللوحة (إنشاء · شرائحها الستّ بأعدادها الحقيقية · جدولة · إرسال
> الآن · نسخة اختبار · إلغاء بسبب · تقريرٌ بالروابط والناس والأحداث)، وصفحة **`/unsubscribe`**
> في الموقع تقرأ مسار الـAPI نفسه بأربع حالات. والتحقّق الحيّ
> **`verify-campaigns.mjs` 59/59 في 7 أقسام** (بلا بريدٍ يخرج من المساحة). الوثيقة
> [`../MARKETING_CAMPAIGNS_P_M7_IMPLEMENTATION_REPORT.md`](../MARKETING_CAMPAIGNS_P_M7_IMPLEMENTATION_REPORT.md).

> **تحديث 2026-09-18 (جلسة P-M4):** والجزء الرابع — **الاشتراك والتفعيل** — مُنجَزٌ على الفرع نفسه:
> معالجٌ بأربع خطوات في `/onboarding` (الباقة ← المنشأة ← المدير ← التحقّق) ثم **لوحة ترحيب**
> بمهامّ إعدادٍ **مقيسة من القاعدة** (فرع · دليل حسابات · أول فاتورة) ورابطِ كل مهمّة إلى مسارها
> في `apps/staff`. وثلاثة مسارات جديدة (`POST /signup/verify` · `POST /signup/resend` ·
> `GET /signup/status/:email`) مع `GET /signup/plans` الموجود، **ورمانٍ مفصولين**: `code` ستةُ
> أرقام يُخزَّن sha256 وحده ويُرسل بالبريد (٣٠ دقيقة · ٥ محاولات · سقف ٥ إرسالات · دقيقة بين
> إرسالين)، و`token` ثمانيةٌ وأربعون حرفاً يحرس الحالة — **والجواب واحد (404) للعنوان المجهول
> والرمز الخاطئ معاً** فلا يتحوّل مسارٌ عامّ إلى أداة سردِ عناوين. **ولا رخصة تُمنح ذاتياً** (طلبُ
> تفعيلٍ `pending` في طابور المشغّل)، **وفترة التجربة إعدادٌ** لا رقم (`billing.trial_days`)،
> **وحدث بريد منصّيّ** `signup.verify`، **و`/signup/plans` موحَّد** مع `/public/plans` (نفس
> `PublicPlansService`). **وأخطاءٌ حقيقية أُصلحت** — منها `pnpm db:roles` الذي كان يسقط بـ
> `syntax error at or near "$1"` (‏`ALTER ROLE … PASSWORD $1`) و`setupTasks` الذي كان يسأل
> `branches.status` (العمود `is_active`). الدليل: `signup-flow.spec.ts` **16** ·
> `apps/marketing/tests/signup.spec.ts` **12** · **`verify-signup.mjs` 54/54**. الوثيقة
> [`../MARKETING_SIGNUP_P_M4_IMPLEMENTATION_REPORT.md`](../MARKETING_SIGNUP_P_M4_IMPLEMENTATION_REPORT.md).

> **تحديث 2026-09-17 (جلسة P-C3):** والجزء الثالث «الهوية والوصول على المنصة» مُنجَز على
> الفرع نفسه: دليل المستخدمين عبر كل المنشآت وبطاقة `/users/[id]` (عضويات · أدوار · جلسات ·
> إبطال · إعادة تعيين 2FA) ومصفوفة `/roles` **حيّة** (`PUT /platform/roles/:code/permissions`
> بتجاوزات في `platform_settings`) ودعوة مشغّل تُنتج حساباً يدخل فعلاً — بلا ترحيل. الوثيقة
> [`../PLATFORM_CONSOLE_P_C3_IMPLEMENTATION_REPORT.md`](../PLATFORM_CONSOLE_P_C3_IMPLEMENTATION_REPORT.md).
>
> هذه المجلدة مكتوبة **لمحادثة جديدة لا تعرف شيئاً عما سبق**: تقرأ هذا الملف، تختار
> الجزء، وتنفّذه. كل بند فيها مشفوعٌ بمكانه في الكود وبرقمٍ مقيس اليوم.

---

## 1. ما فيها

| الملف | السؤال الذي يجيب عنه |
|---|---|
| [`AUDIT_PHASES_01_04.md`](./AUDIT_PHASES_01_04.md) | هل نُفّذت المراحل 1–4 بنفس طريقة 5–11؟ (الجواب: لا — والفجوة موثّقة بالأرقام) |
| [`INCOMPLETE_INVENTORY.md`](./INCOMPLETE_INVENTORY.md) | كل ما لم يكتمل: المراحل، والشاشات، والأسطح الأربعة، والديون التقنية |
| [`PLATFORM_CONSOLE_PLAN.md`](./PLATFORM_CONSOLE_PLAN.md) | خطة لوحة تحكم المنصة (الاحترافية والشاملة): 12 جزءاً + خدمة البريد وخدمة التنبيهات |
| [`MARKETING_SITE_PLAN.md`](./MARKETING_SITE_PLAN.md) | خطة الموقع التسويقي + نظام إدارة المحتوى (CMS) الذي يُدار من لوحة المنصة |

اقتراح القراءة للجلسة الجديدة: هذا الملف ← ملف الجرد ← ملف الخطة التي ستبدأ بها.

---

## 2. تهيئة البيئة (بنفس الترتيب)

```bash
corepack enable                      # لا pnpm بلا corepack في هذه البيئة
pnpm install --no-frozen-lockfile
pnpm -r --filter "./packages/*" build   # إلزامي قبل أي أمر يعتمد على الحزم
pnpm db:local                        # PostgreSQL مضمّن (لا docker ولا psql هنا)
pnpm db:migrate                      # حتى 0065
pnpm db:seed                         # من apps/api
pnpm dev                             # API :3000 · staff :3001 · portal :3002 · platform :3003 · marketing :3004
```

- الخوادم تُشغَّل بأداة العمليات (`start_process`) لا بـ `nohup`؛ واجهات Next على `0.0.0.0`.
- **لا تحاول تثبيت Docker أو Postgres** في بيئة العمل؛ التشغيل الحقيقي عند المستخدم عبر
  `infrastructure/docker-compose.yml` (فيه: postgres · redis · minio · mailhog).
- **لا تشغّل `next build` وخادم Next يعمل** — تعارض معروف؛ التعافي بحذف `.next`.
- الحسابات: `owner@demo.test` على المستأجر `demo`، وكلمات المرور في `.env`
  (`DEMO_OWNER_PASSWORD` · `DEMO_ACCOUNTANT_PASSWORD` · `DEMO_CASHIER_PASSWORD` ·
  `PLATFORM_ADMIN_EMAIL`).

---

## 3. حدود ثابتة من صاحب المشروع (لا تُخالف)

1. **العربية أولاً** في الواجهة وفي التواصل؛ الإنجليزية خيارٌ ثانٍ.
2. **كل شاشة جديدة مطابِقها من `Desktop_ERP` مذكور نصّاً** بملفه ورقم سطره، والتسميات
   العربية تُؤخذ من تلك الملفات **بالحرف** (نسبة تطابق ≥ 90٪)، وأي تسمية مخترَعة تُبرَّر
   صراحةً في جدول «ما اخترعناه» وفي ملخّص الجزء.
3. **لا `git add -A` أبداً**: تُدرَج المسارات صراحةً، ثم `git status --short | grep -v '^??'`
   للتأكد، ثم فحص `gh api repos/.../pulls/4/files` أن لا شيء من `apps/admin*` أو
   `apps/customer*` دخل في الطلب.
4. **كل شاشة جديدة طريقٌ حقيقي**: `apps/staff/tests/navigation.spec.ts` يثبت أن كل بند
   `ready` له صفحة موجودة، وأن كل `/reports/` بصلاحية `reporting.view`.
5. **🇪🇬 مصر خارج النطاق بقرارٍ صريح** (`frmEtaSetting` · `EtaService` ·
   `EtaReciptService`) ونداءا 🚫 إلغاء الفاتورة و❌ رفض الفاتورة تبعاً له؛ ويُسجَّل ذلك
   «مؤجَّلاً/خارج النطاق» لا محذوفاً.
6. **🇸🇦 النظام موجّه للسعودية** (زاتكا)؛ وكل ما عداها يُؤجَّل ولا يُبنى.
7. العمل كله على الفرع `arena/01a0889e-cloud-saas-erp`، وتعليقٌ عربي واحد لكل جزء على
   PR #4.

---

## 4. تعريف «تم» لأي جزء (بوابات المراحل 5–11 — تُطبَّق على ما بعدها)

1. **المصدر بالسطر** — جدول مصادر يسمّي `Form_WPF/*.xaml` و`.xaml.cs` و`Class/*.cs`
   و`Reports/*.repx` بأرقام الأسطر. (في مشروع اللوحة والموقع: تُستبدل مصادر الديسكتوب
   بملفات هذا المستودع + مواصفات الخدمة المنشورة، ويُسمّى ذلك صراحةً).
2. **التسمية بالنص** والتبرير لكل تسمية مخترَعة.
3. **نداءٌ حقيقي** لا محاكاة: نقطة نهاية تعمل، ومهايئ خدمة حقيقي، ومفتاح 🧪 محاكاة
   يُبقي الاختبارات بلا شبكة.
4. **ترحيلٌ مرقَّم + مخطط Drizzle**، و`RLS` بـ`ENABLE` + `FORCE` وسياسة `tenant_id`.
5. **صلاحيات من سجل قائم** (`packages/contracts/src/permissions.ts`: 145 رمزاً اليوم)؛
   والرمز الجديد يحتاج ثلاثة أشياء معاً: إعلاناً في السجل، وإدراجاً idempotent في ترحيل،
   واختبار `apps/api/src/permission-codes.spec.ts` يثبت تطابقهما.
6. **اختبار API جديد + سكربت تحقّق حيّ قابل لإعادة التشغيل** (يصوّر الحالة ويعيدها،
   ولا يترك أثراً).
7. **شاشة حقيقية** في الشجرة بحالة `ready`، وبناء Next أخضر.
8. **أربع وثائق + README الوحدة**: وثيقة المرحلة، خريطة الطريق، README المجلدة،
   `docs/STATUS.md`، وREADME الوحدة.
9. **دفعة بمسارات صريحة** + تعليق عربي على PR #4 بالأرقام والجدول.

---

## 5. أرقام اليوم (مقيسة، لا مرويّة)

| البند | القيمة |
|---|---|
| اختبارات API | **1229** · 143 ملفاً |
| اختبارات staff | **37** (منها 21 لشجرة الملاحة) · contracts **169** في 19 ملفاً (منها **16** لعقود التحليلات P-C12 و**12** للمطوّر P-C11 و**11** للنسخ P-C10) · platform-admin **24** (المسارات 14) · marketing **41** في 5 ملفات · اختبارات الأجزاء: التحليلات **10** · المطوّر **16** · النسخ **25** · العمليات **12** · الدعم **14** · الإعلانات **11** · **التقرير الأسبوعي 13** · **الباقات العامة (P-M3) 9** · **التسجيل (P-M4) 16** |
| الترحيلات | حتى **0079** — `0079_signup_verifications.sql` (P-M4: جدول تحقّق التسجيل بنطاق منصة · RLS مُفعَّل ومفروض · فهرسٌ فريد جزئيّ على البريد المعلَّق) · و`0077`/`0078` محتوى وصلاحياته (P-M5) · `0076_analytics_permission.sql` (P-C12، **صلاحيةٌ بلا جدول**: صفّ `console.analytics.view` + `NOBYPASSRLS`) · `0075_developer_platform.sql` (P-C11: `api_keys` بلا نصٍّ صريح · `webhook_endpoints` بسرٍّ مشفَّر · `webhook_deliveries` · `api_key_uses` + RLS + منح التسلسل + رمزان جديدان) · `0074_platform_backups.sql` (P-C10: `backup_jobs` · `backup_artifacts` · `data_requests` + RLS + `REVOKE UPDATE, DELETE ON audit_log` + صفّ الصلاحية) · `0073_jobs_manage_permission.sql` (P-C9، **صلاحيةٌ بلا جدول**: صفٌّ في `permissions` + `NOBYPASSRLS`) · `0072_support_desk.sql` (P-C8) · و0071 الإعلانات (P-C7) · و0070 خدمة البريد (P-C6) · و0069 قياس (P-C5) · و0068 فوترة (P-C4) · و0067 بطاقة العميل (P-C2) · و0066 لـP-C1 |
| الرموز الصلاحية | **145** رمز مستأجر + **26** رمز لوحة (بينها رمزا البريد P-C6 و`console.notifications.manage` في P-C7 و`console.jobs.manage` في P-C9 و`console.backups.manage` في P-C10 و`console.apikeys.manage` و`console.webhooks.manage` في P-C11 و**`console.analytics.view` في P-C12** و`console.leads.view`/`console.leads.manage` في P-M6 و**`console.campaigns.manage` في P-M7** — و**كلها مستعملة**) في `packages/contracts/src/permissions.ts` |
| التقارير | **102** مفتاح في `apps/api/src/modules/reporting/report-catalog.ts` |
| الشاشات (staff) | **233**: 232 `ready` · 0 `api` · 1 `planned` (يقيسها `screenCounts()` في `tests/navigation.spec.ts`) — **آخر قلبٍ من `api` إلى `ready` في §R11** |
| لوحة المنصة | **34** صفحة (منها **`/leads` P-M6** و**`/campaigns` P-M7**)، و**114** مساراً (**139** عملية) تحت `/api/v1/platform/*` في وثيقة OpenAPI (قشرة 5 · عملاء 15 · هويّة 11 · فوترة 21 · `platform` 5 · استخدام 2 · بريد 13 · إعلانات 5 · دعم 9 · عمليات 8 · **نسخ وبيانات 13** · **مطوّر 12** · **تحليلات 4**؛ ومعها **5** مسارات تسجيل ذاتي **خارج** اللوحة عند `/signup*`، و**3** مسارات تكامل `/integration/v1/*` بمفتاح API)؛ كل مسار برمز `console.*`، **ونهايتا محتوى موقّعتان عامّتان مُعلَنتان بالاسم في `permission-codes.spec.ts`** (P-C1 ✅ · P-C2 ✅ · P-C3 ✅ · P-C4 ✅ · P-C5 ✅ · P-C6 ✅ · P-C7 ✅ · P-C8 ✅ · P-C9 ✅ · P-C10 ✅ · P-C11 ✅ · **P-C12 ✅**) |
| الموقع التسويقي | **29** صفحة في الشجرة (**18** عربية · **10** إنجليزية · قالب `not-found-view`) ومعها `robots`/`sitemap` — ومنها **`/demo` (P-M6)** و**`/unsubscribe` (P-M7)** — والأخيرة خارج خريطة الموقع عن قصد، و**P-M1 · P-M2 · P-M5 · P-M3 · P-M4 · P-M6 · P-M7 ✅** — والباقي: P-M8…P-M10. والاختبارات **57** في 7 ملفات والتقارير الستّة (`verify-marketing-site` 37 · `verify-content` 62 · `verify-pricing` 53 · **`verify-signup` 54** · **`verify-leads` 58** · **`verify-campaigns` 59**) |
| بوابة العميل | 10 صفحات، و12 نقطة نهاية (`/portal/*`) |
| سكربتات التحقّق الحيّ | **44** سكربتاً في `scripts/verify-*.mjs` (**1169** نقطة في **135** قسماً: **218** لسطح اللوحة + **49 للتحليلات** + 63 لبوابة المطوّر + 87 للنسخ والاسترجاع + 76 للعمليات + 148 للفوترة + 85 للاستخدام + 74 للبريد + 47 للإعلانات + 63 لمكتب الدعم + **53 للباقات العامة (P-M3)** + **35 للتقرير الأسبوعي** + **54 للاشتراك والتفعيل (P-M4)** + **58 للعملاء المتوقّعين (P-M6)** + **59 للحملات البريدية (P-M7)**) |

---

## 6. الترتيب المقترح للجلسات القادمة

1. **سدّ فجوة المنهج في المراحل 1–3** (وثيقة مصادر + سكربت تحقّق لكل مرحلة) — تكلفة
   منخفضة، وأثرها أن كل مرحلة قابلة للتدقيق مثل 5–11 (`AUDIT_PHASES_01_04.md` §6).
2. **إكمال المرحلة 04** (تعليق الفواتير · التعدد في الدفع · حساب الفروق · صلاحيات
   الكاشير) — مكاسب تشغيلية لنقطة البيع.
3. **لوحة تحكم المنصة** — **اكتملت أجزاؤها الاثنا عشر كلها** (الأساس ✅ + العملاء ✅ + الهوية ✅ +
   الفوترة ✅ + الاستخدام والحصص ✅ + البريد ✅ + الإعلانات ✅ + الدعم والدخول المؤقّت ✅ +
   العمليات ✅ + البيانات والاسترجاع ✅ + بوابة المطوّر ✅ + **التحليلات ✅**) — ويبقى مؤجَّلاً
   صراحةً: **المستأجر التجريبي (sandbox)** · **أفواج الإيراد** (تحتاج تخصيصاً شهرياً للترخيص) ·
   تصدير Excel/PDF. و**التقرير الأسبوعي بالبريد — نُفِّذ** (2026-09-18): قالبُ P-C6 ومجدولٌ في
   العامل ومفاتيحُ إعدادٍ أربعة، و٤ قراراته التشغيلية مكتوبة في تقريره (البند 3 أدناه).
4. **الموقع التسويقي + نظام المحتوى** — **P-M1 ✅ · P-M2 ✅ · P-M5 ✅ · P-M3 ✅ · P-M4 ✅ ·
   P-M6 ✅ · P-M7 ✅** (الباقات تقرأ من منصة الفوترة مباشرةً، والاشتراك الذاتي يُنشئ منشأةً
   كاملة وطلبَ تفعيل، والعميل المتوقَّع يُلتقط ويُحوَّل، والحملات البريدية تُرسل وتُقاس
   وتُخرج من القائمة بنقرة)، والتالي بترتيب §10 من خطته: **P-M8 + P-M9** ثم **P-M10**.
5. **🇸🇦 فوترة زاتكا: توقيع XAdES** يبقى مؤجَّلاً حتى يتوفّر اعتمادٌ حقيقي — مرتبط
   بالاعتماد لا بالكود.

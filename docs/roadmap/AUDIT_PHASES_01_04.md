# فحص المراحل 1–4 — هل نُفّذت بطريقة 5–11؟

> تاريخ الفحص: 2026-09-17 · الفرع `arena/01a0889e-cloud-saas-erp` · حتى الدفعة `a5c998d`
> (المرحلة 11 — الجزء السادس 📱 واتساب).
>
> **الجواب المختصر: لا.** المراحل 1–3 نُفّذت كـ«مراحل هندسة وإصلاح محرّك» قبل أن يُولد
> منهج المطابقة، والمرحلة 4 هي الجسر (فيها سكربت تحقّق وترحيل ورموز خطأ)، ومن 5 فصاعداً
> صار المنهج ملزِماً. الفجوة ليست في جودة الكود — محرّك الترحيل في المرحلتين 2 و3 من
> أفضل ما في المشروع — بل في **قابلية التدقيق**: لا يمكنك اليوم أن تثبت أن شاشة فاتورة
> البيع مطابقة لنافذة `frmInvSale` لأن الوثيقة لا تسمّي سطراً واحداً منها.

---

## 1. أيّ ترقيمٍ نقصد؟ (تنبيه قبل كل شيء)

في هذا المستودع **ترقيمان** مختلفان، والخلط بينهما يفسد أي فحص:

| الترقيم | المجلد | المدى | محرّكه |
|---|---|---|---|
| **مراحل المطابقة** (parity) | `docs/desktop-parity/` | 00 → 11 | نقل نوافذ `Desktop_ERP` إلى السحابة؛ الوثيقة = مواصفة مطابقة |
| **مراحل بناء السحابة** | `docs/PHASE_01…23_IMPLEMENTATION_REPORT.md` + `docs/phases/PHASE_.._PROMPT.md` | 01 → 23 | بناء المنصة نفسها (الهيكل، النِست، الهوية، الخدمات، المحاسبة…)؛ الوثيقة = تقرير تنفيذ |

هذا الفحص عن **مراحل المطابقة 1–4** (`PHASE_01_USERS_NAV` · `PHASE_02_SALES_ENGINE` ·
`PHASE_03_PURCHASE_ENGINE` · `PHASE_04_POS_SHIFTS`) مقارنةً بـ **5–11**، لأن سؤالك جاء
في سياق العمل الجاري على المرحلة 11. وللأمانة: مراحل بناء السحابة P01–P04 (الهيكل ·
نواة المنصة · الهوية والصلاحيات · خدمات المنصة) لها منهجها الخاص الموثّق في
`docs/AI_DEVELOPMENT_PROTOCOL.md` §8 (تقرير تنفيذ لكل مرحلة + ترحيل بملف `down/` +
تحقّق على حقيقية Postgres)، وهي **مكتملة بمنهجها** ولا تدخل في هذا الفحص.

---

## 2. مِقياس المقارنة — بوابات المراحل 5–11 كما تُمارَس فعلاً

| # | البوابة | الدليل عليها في الكود |
|---|---|---|
| 1 | **المصدر بالسطر** — جدول مصادر يسمّي `Form_WPF/*.xaml` و`.xaml.cs` و`Class/*.cs` و`Reports/*.repx` بأرقام الأسطر | `PHASE_11_EINVOICING.md` §1 (187 إحالة سطر) |
| 2 | **التسمية بالنص** — تسميات عربية من تلك الملفات بالحرف، ونسبة تطابق ≥ 90٪، والمخترَع مبرَّر في جدول | `PHASE_11_EINVOICING.md` §4.5 · §5.5 · §6.5 · §7.5 · §8.5 |
| 3 | **الأجزاء** — كل جزء = نافذة أو عنقود متجانس، ولكل جزء قسمه | `PHASE_11_EINVOICING.md` §3 + §4…§8 |
| 4 | **نداءٌ حقيقي** — نقطة نهاية تعمل ومهايئ خدمة حقيقي، مع مفتاح 🧪 محاكاة | `modules/integrations/whatsapp/cloud-api.ts` · `modules/payments/gateways/*` |
| 5 | **ترحيل + مخطط + RLS** (`ENABLE` + `FORCE`) | `0065_whatsapp_messages.sql` |
| 6 | **صلاحيات من سجل قائم** (أو رمز جديد مبرَّر بإعلان + ترحيل + اختبار) | `whatsapp.controller.ts`: `tenant.settings.manage` · `sales.view` |
| 7 | **اختبار API جديد + سكربت تحقّق حيّ قابل لإعادة التشغيل** | `test/whatsapp-invoice.spec.ts` (17) · `scripts/verify-whatsapp.mjs` (71) |
| 8 | **طريق حقيقي** في الشجرة: `ready` + الصفحة موجودة + `navigation.spec.ts` أخضر | `apps/staff/app/settings/whatsapp/page.tsx` |
| 9 | **أربع وثائق + README الوحدة** | المرحلة · الخريطة · README المجلدة · `docs/STATUS.md` · README الوحدة |
| 10 | **دفعة بمسارات صريحة + تعليق عربي** | `a5c998d` · تعليق PR #4 |

---

## 3. القياس (أرقام مأخوذة اليوم، وأمر إعادة قياسها تحتها)

| المرحلة | أسطر الوثيقة | إحالات أسطر `Desktop_ERP` | حروف عربية | سكربت تحقّق حيّ | ملف اختبار مخصّص | ترحيل |
|---|---:|---:|---:|---|---|---|
| 01 المستخدمون والصلاحيات | 45 | **0** | **0** | ✗ | `staff/tests/navigation.spec.ts` (20 اختباراً تتحقّق من وجود الصفحات فقط) | — |
| 02 محرّك فواتير البيع | 112 | **0** | 5 | ✗ (تحقّق يدوي في §2F بأرقام ثابتة) | `api/test/sales-posting.spec.ts` (8) | — |
| 03 محرّك المشتريات | 101 | **0** | 27 | ✗ (تحقّق يدوي في §3D) | `api/test/purchase-posting.spec.ts` (8) | — |
| 04 نقطة البيع والورديّات | 180 | 2 | 56 | ✓ `scripts/verify-pos.mjs` | `api/test/pos-checkout.spec.ts` (8) | `0033` |
| 05 المخزون | 769 | 30 | 5,650 | ✓ `verify-inventory.mjs` | 6 ملفات | 0040–0045 |
| 06 الخزينة | 736 | 31 | 20,276 | ✓ `verify-treasury.mjs` | 7 ملفات | 0046–0048 |
| 07 المحاسبة | 575 | 18 | 17,710 | ✓ `verify-accounting.mjs` | 6 ملفات | 0045–0048 |
| 08 الموظفون والرواتب | 588 | 47 | 17,193 | ✓ `verify-hrm.mjs` | 6 ملفات | 0049–0051 |
| 09 الوحدات الرأسية | 1,206 | 105 | 33,958 | ✓ 9 سكربتات | 11 ملفاً | 0052–0060 |
| 10 التقارير | 1,096 | 123 | 32,940 | ✓ 7 سكربتات | 8 ملفات | 0061 |
| 11 الفاتورة الإلكترونية | 601 | **187** | 15,407 | ✓ 5 سكربتات | 5 ملفات | 0062–0065 |

أوامر إعادة القياس:

```bash
cd docs/desktop-parity
wc -l PHASE_*.md
grep -oE '\bL[0-9]{2,4}\b' PHASE_0*_*.md | wc -l          # إحالات الأسطر
python3 -c "import io,re;[print(f, len(re.findall(r'[؀-ۿ]', io.open(f,encoding='utf-8').read()))) for f in ['PHASE_01_USERS_NAV.md','PHASE_11_EINVOICING.md']]"
ls scripts/verify-*.mjs | wc -l
```

---

## 4. الحكم بنداً بنداً

### 4.1 المرحلة 01 — المستخدمون والصلاحيات (`PHASE_01_USERS_NAV.md`)

| البوابة | الحالة | الملاحظة |
|---|:--:|---|
| 1 المصادر بالسطر | ✗ | يذكر `frmAddUsers*` · `frmUsersPermissions*` · `frmOperPermission*` · `Class/User.cs` · `Class/FormPermission.cs` **بلا سطر واحد** |
| 2 التسمية بالنص | ✗ | **صفر حرف عربي** في الوثيقة كلها |
| 3 الأجزاء | 🟡 | قسمان (1A/1B) لا علاقة لهما بالنوافذ |
| 4 نداء حقيقي | ✓ | CRUD حقيقي على `/memberships` و`/roles` والشاشات تعمل (367 + 246 سطراً) |
| 5 ترحيل/RLS | — | لا يحتاج |
| 6 صلاحيات | ✓ | الأفعال الخمسة (view/create/edit/delete/print) موثّقة |
| 7 اختبار + سكربت | ✗ | لا سكربت تحقّق؛ `navigation.spec.ts` يتحقّق من وجود الملفات لا من السلوك |
| 8 طريق حقيقي | ✓ | `/settings/users` · `/settings/roles` |
| 9 الوثائق | ✗ | لا جدول مصادر، لا «ما أُجِّل»، لا معايير قبول مقيسة |
| 10 الدفعة | ✓ | — |

**ما ناقصه فعلاً:** النافذة `FormPermission.cs` في الديسكتوب تمنح الصلاحية **لكل نموذج**
(Editable/Addable/Delete/Search/Print)، والسحابة تمنحها **لكل رمز** — وهذا فرقٌ في
المنتج لا في التوثيق، ولم يُوثَّق كقرار. كذلك لا يوجد اختبار يثبت أن دوراً بعينه يرى
ما يجب أن يراه على الشاشتين.

### 4.2 المرحلة 02 — محرّك فواتير البيع (`PHASE_02_SALES_ENGINE.md`)

| البوابة | الحالة | الملاحظة |
|---|:--:|---|
| 1 المصادر بالسطر | ✗ | `Class/InvoiceCalc.cs` و`SaveInvoice`/`BindToEntry` مذكورة بلا أسطر |
| 2 التسمية بالنص | ✗ | 5 حروف عربية في 112 سطراً (اسم حساب «فرق متوسط التكلفة» فقط في 03) |
| 3 الأجزاء | 🟡 | 2A–2F: أقسام هندسية لا نوافذ |
| 4 نداء حقيقي | ✓ | `POST /sales/invoices/:id/post` محرّكٌ واحد، ورموز الخطأ موثّقة |
| 5 ترحيل/RLS | — | لا يحتاج |
| 6 صلاحيات | ✓ | رموز قائمة |
| 7 اختبار + سكربت | 🟡 | `sales-posting.spec.ts` (8) جيد؛ **لكن لا سكربت تحقّق حيّ** — §2F أرقامٌ ثابتة من 2026-09-10 لا يمكن إعادة إنتاجها بضغطة |
| 8 طريق حقيقي | ✓ | `sales/invoices` و`[id]` و`new` |
| 9 الوثائق | ✗ | لا جدول مصادر ولا «مؤجَّل» بمعنى المطابقة (الموجود متابعات هندسية) |
| 10 الدفعة | ✓ | — |

**ما ناقصه فعلاً:** النافذة `frmInvSale` لم تُفتح للمطابقة أصلاً — لا جدول أعمدة، ولا
تسميات («فاتورة مبيعات» · «العميل» · «طريقة الدفع» · «ترحيل» · «طباعة»)، ولا قرار
موثّق فيما استُبدل منها. ومتابعات الوثيقة نفسها ما زالت مفتوحة: `SALES_VOID_HAS_RETURNS`،
و`apps/staff/lib/posting.ts` (بنّاءات لم تعد مستخدمة).

### 4.3 المرحلة 03 — محرّك المشتريات (`PHASE_03_PURCHASE_ENGINE.md`)

نفس حكم 02 تماماً (0 إحالة سطر · 27 حرفاً عربياً · بلا سكربت تحقّق)، ومعها متابعتان
مفتوحتان: `GET /purchase-invoices/:id` لا يعيد الدفعات، وإبطال مشترياتٍ عليها مردودات
مسموح بلا حاجز.

### 4.4 المرحلة 04 — نقطة البيع والورديّات (`PHASE_04_POS_SHIFTS.md`)

**هذه هي الجسر**: أول وثيقة فيها جدول «سلوك الديسكتوب ← السحابة قبل/بعد»، وترحيل
مسمّى (`0033_pos_shift_link.sql`)، ورموز خطأ بعباراتها، و`scripts/verify-pos.mjs`،
وقسم متابعات صريح. ما ينقصها: إحالة أسطر (2 فقط: `frmPOS.DeleteInv` L1828)، والتسميات
العربية (56 حرفاً)، ولا قسم «ما اخترعناه».

---

## 5. لماذا حدث الفرق؟ (قراءة منصفة)

المراحل 1–3 لم تكن مطابقة نوافذ بل **إصلاح محرّك**: كانت السحابة تسمح للمتصفح ببناء
القيود وحركات المخزون، فنُقل الحكم إلى الخدمة (`post()` يحسب من ملف الترحيل، لا ممّا
يرسله المتصفح). هذا عملٌ صحيح ومهم، لكن وثيقته **تقرير هندسي** لا **مواصفة مطابقة**.
منهج 5–11 وُلد لاحقاً مع بداية نقل النوافذ نفسها (`PHASE_05_INVENTORY.md` أول وثيقة
فيها 30 إحالة سطر و5,650 حرفاً عربياً)، ثم اشتدّ في 9 و10 و11.

الخلاصة: **الكود في 1–3 ليس أضعف، لكنه غير قابل للتدقيق على مستوى النافذة** — ولا أحد
يستطيع اليوم أن يجيب: «هل شاشة فاتورة البيع مطابقة لـ`frmInvSale`؟».

---

## 6. خطة السدّ (أربعة أجزاء، كل جزء جلسة)

كل جزء يُنفَّذ ببوابات §2 نفسها، ويُختم بسكربت تحقّق حيّ ودفعة بمسارات صريحة.

### R1 — مطابقة شاشتي المستخدمين والصلاحيات (تكملة المرحلة 01) — ✅ مُنجَز (2026-09-19)

- **المصادر:** `Form_WPF/frmAddUsers.xaml(+.cs)` · `frmUsersPermissions*` ·
  `frmOperPermission*` · `Class/User.cs` · `Class/FormPermission.cs` — تُقرأ سطراً
  بسطر، ويُبنى جدول: النموذج × الأفعال الخمسة.
- **الناتج:** قسم § في `PHASE_01_USERS_NAV.md` بجدول مصادر وجدول تسميات، وقرار موثّق في
  الفرق بين «صلاحية لكل نموذج» (الديسكتوب) و«صلاحية لكل رمز» (السحابة)،
  و`scripts/verify-identity-rbac.mjs` (≈ 50 نقطة: دعوة مستخدم، نطاق فرع، مصفوفة
  الصلاحيات، رفض بلا رمز، عزل المستأجر).
- **مخترَع يُبرَّر:** شاشة «نطاق الفرع» و«حدّ الخصم» بديلاً عن `OperMaxDiscount`.
- **النتيجة:** `docs/desktop-parity/PHASE_01_USERS_NAV.md` §R1 (مصادر بأسطرها · جدول
  النموذج × الأفعال الخمسة · OperNo 1..20 ← الرموز · «ما اخترعناه» · المؤجَّل) ·
  `scripts/verify-identity-rbac.mjs` **78/78 في 9 أقسام** (تشغيلٌ متعاقب يعيد الحدود إلى
  خطّ أساسها) · `apps/api/test/identity-rbac.spec.ts` **18/18** · ترحيل **`0085`**
  (`memberships.max_discount_pct` / `max_discount_amount`) · و«تجاوز الخصم الافتراضي»
  صار رمزاً: `sales.discount.override`.

### R2 — مطابقة نافذة فاتورة البيع (تكملة المرحلة 02) — ✅ مُنجَز (2026-09-20)

- **المصادر:** `Form_WPF/frmInvSale.xaml` (+ `.xaml.cs`) و`Class/ItemOper.cs`
  (`CalcTotal`@1533) و`Class/InvoiceOper.cs` (`SaveInvoice`@1310 · `SendZatca`@2209 ·
  `BindToEntry`@2252 · `DeleteInvoice`@4419) و`Class/InvoiceCalc.cs`.
- **الناتج:** **§R2** في `docs/desktop-parity/PHASE_02_SALES_ENGINE.md` بجدول مصادر
  بأسطرها، وجدول تسميات حرفيّ (25 نصّاً) مطبَّق على `sales/invoices/new` و`[id]`،
  و`scripts/verify-sales-engine.mjs` **40/40 في 9 أقسام**، وحارسا سلسلة المستندات
  (`SALES_VOID_HAS_RETURNS` · `SALES_REFERENCE_VOIDED`) مع سبيكين جديدين
  (`sales-posting.spec.ts` **10/10** ⇒ api **1307**) — و«ما اخترعناه» بسبعة قرارات
  (منها «🗑️ حذف» ⇒ «🗑️ إلغاء الفاتورة»، و`apps/staff/lib/posting.ts` أُغلق بالتحقّق:
  البنّاءات الميتة غير موجودة أصلاً).
- **مؤجَّل صراحةً:** `📝 البيان` · `📦 الباركود` · `📊 مركز التكلفة` · `🔢 التسلسلي`
  (لا حقول لها في `SalesInvoiceInput`) · زرُّ `🔄 مرتجع` في الشاشة (المسارُ مختبر) ·
  نسبُ خصم الرأس · `📋 عرض سعر`/`👁️ معاينة`/`🖨️ طباعة 2`/`🧾 سند قبض`.

### R3 — مطابقة نافذة فاتورة المشتريات (تكملة المرحلة 03) — ✅ مُنجَز (2026-09-20)

- **المصادر:** `Form_WPF/frmInvPurch.xaml` (1454 — النافذة المرجعية: Title@7 · الحقول
  L325–L550 · أعمدة البنود L665–L867 · الملخّص L920–L1026 · الإجماليات L1056–L1101 ·
  الأزرار L1396–L1437) و`.xaml.cs` (2879 — خيارات الدفع L191–193 ورسائل التحقّق
  L1200–L1275) و`frmPurchInv.xaml` (1418 — النسخة المبسّطة) و`Class/ItemOper.cs`
  (`CalcTotal`@1533 · `AvgCost`@162 · `ItemAdditonalCos`@2086).
  *وتصحيحٌ للسطر أعلاه:* لا وجود لـ`frmInvPurchase*`؛ النافذة اسمُها `frmInvPurch`.
- **الناتج:** **§R3** في `docs/desktop-parity/PHASE_03_PURCHASE_ENGINE.md` — جدول مصادر
  بأسطرها، وجدول تسميات حرفيّ مطبَّق على `purchases/invoices/new` و`[id]`
  («📋 المرجع» · «👤 اسم المورد» · «🏪 المستودع» · «💳 طريقة الدفع» بخياراتها الثلاثة ·
  «📋 بنود الفاتورة» وأعمدتها · «🖨️💾 حفظ مع طباعة» · «🖨️ طباعة»)، وحارسا سلسلة
  المستندات (`PURCHASE_VOID_HAS_RETURNS` · `PURCHASE_REFERENCE_VOIDED`)، والدفعات في
  `GET /purchase-invoices/:id`، و`scripts/verify-purchase-engine.mjs` **41/41 في 10
  أقسام** (تشغيلان متعاقبان)، وسبيكات `purchase-posting.spec.ts` **11/11**.
- **عيبٌ حقيقي أصلحه السكربت (لا عيبَ في السكربت):** عكس الإدخال كان يصرف بمتوسط اليوم
  لا بسعر الإدخال، فتعلَق قيمةٌ في المخزون بعد إلغاء فاتورة شراء (2821.7489 بدل 1920) —
  أُضيف تقييم `outAtOriginalCost` ورُبط `nextValue` بالتكلفة المكتوبة في الحركة نفسها،
  فرجع المخزون إلى 48 @40.0000 (فارق تدوير 0.0001 معلَن).
- **مؤجَّل صراحةً:** «📝 البيان» · «📦 الباركود» · «📊 مركز التكلفة» (رأساً) ·
  `تاريخ الإنتهاء` · «بدون ضريبة» كخانةٍ عامّة (بديلها مجموعة ضريبية بنسبة صفر) ·
  «📝 سند قبض» · «👁️ معاينة» · «📥 استيراد» · «⚡ اختصار فاتورة» · فلاتر تبويب «🔍 البحث» ·
  لوحةُ الملخّص (الكمية الحالية · التعادل · كمية و/س · آخر شراء/بيع/المتوسط/المنافس ·
  باركود الصنف) — ثلاثةُ حقولٍ منها نُفِّذت (عدد البنود · إجمالي الكمية · خصم الفاتورة).

### R4 — إكمال نقطة البيع (متابعات المرحلة 04 المعلَنة) — ✅ مُنجَز (2026-09-20)

- تعليق الفواتير واسترجاعها (`frmHoldM*` — 8 خانات) · الأصناف المختصرة
  (`frmShortCutInv*`) · تعدّد طرق الدفع في الفاتورة الواحدة (`frmPOSPay*`) · حساب
  فروق الإغلاق (`EntryOper.BindCloseShiftToEntry` L402) · صلاحيات الكاشير وتجاوز السعر
  (`frmCasherSetting*` · `pos.priceoverride`) · عمود `cashier_id` على `sales_invoices`.
- **الناتج:** قسم §5 مصادر لكل نافذة، وتوسيع `scripts/verify-pos.mjs`، واختبارات جديدة.
- **تصحيحان لهذا الوسم بعد قراءة الكود (وثيقة المرحلة §R4.1):**
  1. **«8 خانات» خطأ** — `HoldList = new int[9]` (`frmPOS.xaml.cs:179`) وتسعة أزرار
     `Hold1Btn`…`Hold9Btn` (`frmPOS.xaml:1135–1169`)، والفهرس `0..8`. شاشةُ التعليق
     **ليست** `frmHoldM` (وهي «🔗 ربط الفواتير بالمراكب»، `frmHoldM.xaml:7`)، بل صفُّ
     الأزرار داخل `frmPOS`؛ والحفظ في الديسكتوب فاتورةٌ `inv_type=3/proc_type=3`
     والسحابة تحفظ **السلة** (`pos_holds.cart`) لأنّ الترقيم عند الترحيل.
  2. **«الأصناف المختصرة» خطأ** — `FrmShortCutInv` «حاسبة الفاتورة» (`:6`)، ناتجُها
     يُدرَج في البنود («✅ إدراج» `:224`)، وهي **مؤجَّلة** بوصفها آلة حساب. بلاطاتُ
     الأصناف السريعة في `frmPOS` هي `FlowItemsPnl`، والسحابة تعرض بلاطات الأصناف فعلاً.
- **ما نُفِّذ:** ٩ خانات تعليق/استرجاع/إفراغ (`pos_holds` · ترحيل `0087`) · تعدّد الدفع
  كمصفوفة تسويات مع «⚖️ F6 مطابقة» والباقي على ذمة العميل · فرض `pos.priceoverride`
  («لا يمكن تعديل السعر» 403) · `sales_invoices.cashier_id` · سبعة مفاتيح `pos.*`
  تقرأها الشاشة بصلاحية `pos.view` وتُكتب من `/settings/general` · «قيد الإغلاق» في
  `/sales/shifts` (`BindCloseShiftToEntry`).
- **فجوةٌ حقيقية كُشفت وأُصلحت:** `ensureEnabled` في خدمات الأفواج يقرأ `pack.<name>`
  وهو مفتاحٌ **ليس في سجلّ `@erp/config`** ⇒ `PUT /settings/pack.pos` يردّ 400 ولا سبيل
  لإطفاء فوجٍ أو إشعاله من الواجهة، والبوابة تمرّ لأنّ الصفّ غائب. صُحّح في `pos` وحده
  (`feature.pos` قارئاً + `pack.pos` توافقاً + تسجيل `pos.requireShift`)؛ وبقيّة الأفواج
  (hrm · projects · marina · optics · tailoring · installments · fitment) **مؤجَّلة**.
- **مؤجَّل صراحةً:** «حاسبة الفاتورة» (`frmShortCutInv`) · «🔗 ربط الفواتير بالمراكب»
  (`frmHoldM` — مارينا) · حدود السعر الدنيا/العليا (`ItemPrices.high/low_sale_price` ·
  `User.PassHeighestSalePrice`/`PassLowestSalePrice`، `frmPOS.xaml.cs:1391–1416`) ·
  تفصيل «🏧 ATM»/«💳 فيزا»/«🍽️ ضيافة» كطرقٍ مستقلّة · قيم التوصيل/التأمين/الوحدة
  الافتراضية (المفاتيح مُسجَّلة ومقروءة، واستعمالها في شاشة التوصيل مؤجَّل) ·
  ضبط الرايات من `/settings` (المسار القائم: لوحة المنصة §4).
- **الأرقام:** `apps/api/test/pos-checkout.spec.ts` **12/12** (كان 8) ·
  `scripts/verify-pos.mjs` **19 فحصاً** (كان 12)، تشغيلان متعاقبان · سويت الـAPI
  **1314/149 ملفاً** (كان 1310) · contracts 232 · staff 37 · database 17 · config 10 ·
  tsc/eslint/بناءا (api · staff · packages) ✓.

---

## 7. ما يُعاد استخدامه (لا تخترع)

- **قالب السكربت:** `scripts/verify-pos.mjs` (للأجزاء R2/R3) و`scripts/verify-whatsapp.mjs`
  (الأحدث: تصوير الإعدادات وإعادتها، 11 قسماً، 71 نقطة).
- **قالب الوثيقة:** `docs/desktop-parity/PHASE_11_EINVOICING.md` §1–§3 (مصادر · جدول
  الانتقال · الأجزاء) ثم §«ما اخترعناه» ثم §الاختبارات.
- **قالب الاختبار:** `apps/api/test/whatsapp-invoice.spec.ts` (ممثّلون بصلاحيات حقيقية
  + عزل مستأجر + 🧪 محاكاة).
- **بوابة إضافية قبل أي تسمية جديدة:** `apps/staff/tests/navigation.spec.ts` (20 اختباراً)
  يثبت أن الشاشة موجودة فعلاً.

# CR-P21 — Installments and Projects pack

Implemented Phase 21 vertical packs with feature flags, tenant isolation, permissions, APIs, admin routes and docs. Progress bill source semantics are represented by `progress_bills.invoice_id` and standard sale invoices rather than introducing a new invoice kind.

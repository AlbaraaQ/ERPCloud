# المرحلة 06 — الخزينة (Treasury)

مطابقة `Desktop_ERP` سطحاً بسطح، بنفس منهج المرحلة 05: ترحيل ← خدمة واختبارات ← شاشة،
وكل شاشة تُبنى من ملف `Desktop_ERP` الحقيقي الذي يقابلها.

---

## 1. ما يفعله الديسكتوب، ملفاً بملف

الخزينة في `Desktop_ERP` ليست شاشة واحدة؛ هي **وثيقة** تتكرر بأسماء مختلفة، و**تعريفات**
(خزن وبنوك)، و**كشف** يُقرأ به الصندوق.

| السلوك | المصدر في `Desktop_ERP` | ما يفعله | ما بنيناه |
|---|---|---|---|
| سند قبض عميل | `Form_WPF/frmSandQ.xaml` — `Title="سند قبض عميل"`، وحقوله `txtNo`، `txtTime`، `cmbBranches`، `cmbClients`، `txtVal`، `cmbCostCenter`، ومجموعة `💼 تفاصيل الدفع` | وثيقة كاملة: من، كم، كيف، ثم التفاصيل — مع كتلة الشيك خلف نوع الدفع | الشاشة `/treasury/vouchers` و`vouchers` بكل حقوله (§2) |
| سند قبض من عميل | `Form_WPF/frmCashVoucherPM.xaml` — `🧾 سند قبض من عميل` | النسخة المختصرة لنقطة البيع | نفس الوثيقة؛ ليس شاشة ثانية |
| سند صرف لمورد | `Form_WPF/frmSandD.xaml` — `Title="سند صرف لمورد"` | مرآة القبض، مع `استلمت أنا السيد` و`يصرف من حساب` و`دفعة لحساب` | نفس الشاشة بتبويب `📤 سند صرف` |
| سند صرف الضريبة | `Form_WPF/frmSandVAT.xaml` — `Title="سند صرف الضريبة"`، `🏧 يصرف من حساب` | سند صرف موجّه للضريبة | جهة `ضريبة القيمة المضافة` في `subtype` |
| سندات قبض / صرف (قوائم) | `frmSandQD.xaml` · `frmSandSD.xaml` — `Header="🔢 الرقم"`، `👤 الموظف`، `💰 المبلغ`، `📅 التاريخ` | بحث بالفترة والرقم والمرجع | 🔍 لوحة البحث في الشاشة + `GET /vouchers?from&to&q` |
| القيد من الوثيقة | `Class/ReceiptOper.cs` L21 `BindReceiptToEntry`، وL159 `SaveReceipt` | **يُقيَّد السند مع حفظه**: `entry.Note = Receipt.Notes`، ومركز التكلفة والمندوب على سطر العميل | الخدمة تبني القيد عند الترحيل إن لم يزوَّد به (§3) |
| ⏰ الوقت | نفس الملف: `Receipts.ReceiptDate` تاريخ **وساعة** | كشف الصندوق يُرشَّح `من وقت / إلى وقت` | `vouchers.voucher_time` |
| 👔 المندوب | `Receipts.SalesManID`، و`account.salesman = Receipt.SalesManID` على سطر القيد | العمولة وتحليل المبيعات يريان من حصّل | `vouchers.salesman_id` → `employees` |
| 📝 البيان | `Receipts.Notes` | يُنسخ إلى بيان القيد | `vouchers.description` ووصف القيد |
| حالة الشيك | `frmPaymentVoucher.xaml` — `تحت التحصيل` · `محصّل` · `مرتجع` | الشيك وعد لا نقد | `chequeState` + قيد التحصيل وقيد الارتجاع (§3) |
| تحويل بنكي | `Form_WPF/frmPayBank.xaml` — `🏦 اختر طريقة الدفع (تحويل بنكي)`، `🏦 البنوك المتاحة` | اختيار البنك لحظة الدفع | الجزء الخامس: بلاطات الاختيار + سطر بنكي لكل بنك في الإغلاق (§12) |
| تعريف الخزن | `Form_WPF/frmTreasury.xaml` — `Title="تعريف الخزن"`، `🏦 اسم الصندوق`، `👤 الموظف المسئول`، `📋 حالة الصندوق`، `⭐ الافتراضي`، `📝 ملاحظات`، و`Stock_Emps` للمسئولين | الصندوق: اسم، فرع، حالة، مسئولون، افتراضي، ملاحظات | الجزء الثاني (§9) |
| تعريف البنوك | `Form_WPF/frmBanks.xaml` — `🏦 تعريف البنوك`، `🌍 الدولة`، `🏙️ المدينة`، `💰 نسبة الاقتطاع %` | بطاقة بنك | الجزء الثاني (§9) |
| بطاقة حساب المصاريف | `Form_WPF/frmExpensesAccount.xaml` — `📒 بطاقة حساب المصاريف` | حساب مصروف + مركز تكلفة | `/accounting/expenses` (قائم) |
| حركة الصندوق | `Form_WPF/frmRptKhzna.xaml` — `Title="حركة الصندوق"`، `📥 وارد`، `📤 صادر`، `⚖️ الرصيد`، `⚖️ الرصيد الإجمالي`، `📅 رصيد الفترة المحددة` | كشف الصندوق برصيد متحرك | الجزء الثالث (§9) |
| إغلاقات اليومية | `Form_WPF/frmCloseShift.xaml` — `📊 إغلاقات اليومية`، `🏦 رصيد الصندوق`، `💵 النقدي`، `💹 الصافي`، `📉 الفرق` | إقفال الوردية | الجزء الرابع: الشاشة `/treasury/day-close` (§11) |

**انحرافان مقصودان عن الديسكتوب:**

1. **الحفظ ليس ترحيلاً.** الديسكتوب يُقيّد السند مع حفظه (`SaveReceipt` يكتب الوثيقة
   والقيد في خطوة واحدة). السحابة تحفظ مسوّدة ثم ترحّلها: المسوّدة تُعدَّل، والترحيل هو
   اللحظة التي تتحرك فيها الأرصدة والدفتر معاً — وبذلك لا يمكن أن يخرج نقد بلا قيد.
2. **الوثيقة واحدة لا أربع.** `frmSandQ` و`frmSandD` و`frmSandVAT` و`frmPaymentVoucher`
   متطابقة إلا في الجهة؛ السحابة تعرضها وثيقة واحدة بمُبدّل جهة، لأن تكرار الشاشة أربعة
   أضعاف يعني أربعة أماكن لنسخة قديمة من القاعدة.

## 2. الجزء الأول — البيانات (الترحيل `0040_treasury_voucher_detail.sql`)

ترحيل جمعي مع ملف `down/` يقابله:

| العمود | المصدر | لماذا |
|---|---|---|
| `vouchers.description text` | `Receipts.Notes` | بيان القيد (`entry.Note`) |
| `vouchers.voucher_time time` | `Receipts.ReceiptDate` (تاريخ + ساعة) | كشف الصندوق يُرشَّح بالساعة |
| `vouchers.salesman_id uuid → employees` | `Receipts.SalesManID` | العمولة وتحليل التحصيل |
| `vouchers.foreign_amount numeric(20,4)` | حقل `💲 دولار` في `frmSandQ` | قيمة السند بعملتها، والمبلغ الأساسي بالريال |

فهارس: `vouchers_location_date_idx` (كشف الصندوق: موقع بين تاريخين) و`vouchers_salesman_idx`.
مفتاح `chequesInHandAccountId` (أوراق القبض) أُضيف إلى مفاتيح ملف الترحيل في
`packages/contracts` — بلا ترحيل، لأن الخريطة JSON.

## 3. الجزء الأول — القاعدة في الخدمة

### 3.1 القيد الذي كان يُبنى يدوياً

كان الترحيل يبني قيداً **إن** زوّده المتصل بسطوره؛ ومن لم يزوّد — مثل `HRM.payRun` حين
يصرف راتباً — خرج النقد من الصندوق بلا قيد أصلاً. من الآن الخدمة تبنيه:

| الجانب | الحساب |
|---|---|
| الصندوق/البنك | حساب موقع النقد (`cash_locations.account_id`)، ثم ملف الترحيل `cashAccountId` / `bankAccountId` حسب نوع الموقع |
| الطرف | الحساب المقابل، ثم حساب العميل المدين أو حساب المورد الدائن، ثم ملف الترحيل `receivableAccountId` / `payableAccountId` |
| الشيك | `chequesInHandAccountId` (أوراق القبض) — لا يلمس الصندوق حتى يُحصَّل |

`CASH_ACCOUNT_REQUIRED` و`COUNTER_ACCOUNT_REQUIRED` هما الرفضان حين لا يجد المحرك حساباً،
والبيان (`description`) هو وصف القيد كما في `entry.Note = Receipt.Notes`.

### 3.2 الشيك

| الحدث | الرصيد | القيد |
|---|---|---|
| ترحيل سند بشيك | **لا يتحرك** — وعد لا نقد | مدين أوراق القبض / دائن العميل |
| تحصيل (`clear`/`collect`) | يتحرك | مدين الصندوق أو البنك / دائن أوراق القبض |
| ارتجاع (`bounce`) | لا يتحرك | مدين العميل / دائن أوراق القبض — يعيد الدين |

الشيك المرتجع **نهائي**: محاولة تحصيله مرة ثانية تُجاب بـ `422 CHEQUE_INVALID_STATE`.

### 3.3 المسوّدة والبحث

- `PATCH /vouchers/:id` يُعدّل المسوّدة كاملة كما في `frmSandQ.xaml.cs:903` (التاريخ،
  المرجع، الصندوق، المندوب، نوع الدفع، كتلة الشيك، مركز التكلفة، البيان) — إلا نوع
  الوثيقة، لأن السلسلة وشكل القيد يُشتقّان منه. المرحَّل يُجاب بـ `409 VOUCHER_IMMUTABLE`.
- `GET /vouchers?from=&to=&q=` — بحث `frmSandQD`/`frmSandSD`: بالفترة، وبالرقم أو رقم
  المرجع أو رقم الشيك أو البيان أو اسم المستلم.
- `⏰ الوقت` يُقبل `9:05` ويُخزَّن `09:05:00`؛ ومناديب مؤسسة أخرى تُرفض
  (`422 SALESMAN_NOT_FOUND`).

## 4. الجزء الأول — الشاشة (`/treasury/vouchers`)

* **بطاقات إحصائية**: عدد السندات، المسودات، المُرحَّل، قيمة المُرحَّل، وشيكات تحت التحصيل.
* **تبويبان**: `📥 سند قبض` · `📤 سند صرف`.
* **🔍 لوحة بحث**: `📅 من تاريخ`، `📅 إلى تاريخ`، `🔍 البحث`، `📋 كل الفترة`.
* **ترويسة الوثيقة** (`📄 بيانات السند`): الرقم (يُرقَّم تلقائياً)، التاريخ، ⏰ الوقت،
  🏢 الفرع، 👤 حساب العميل / 🏭 المورد، 👤 الجهة.
* **المبلغ**: `💵 قيمة السند`، العملة (`💲 دولار`…)، وحقل القيمة بالعملة حين لا تكون الريال.
* **💼 تفاصيل الدفع**: `💳 نوع الدفع` (💵 نقدي / 🏦 شيك / 🏦 تحويل بنكي / 💳 شبكة)، وتنفتح
  `🏦 مسحوب على بنك` و`🔢 رقم الشيك` و`📅 تاريخ استحقاق (شيك)` خلف الأنواع البنكية، ثم
  `📅 تاريخ المرجع`، `🔢 رقم المرجع`، مركز التكلفة، `👔 المندوب`، المستلم، `📝 البيان`.
* **الشبكة**: `🔢 الرقم` · `📅 التاريخ` · `⏰ الوقت` · الطرف · `📝 البيان` · `🏦 الصندوق` ·
  `💳 نوع الدفع` · `💰 المبلغ` · `📋 الحالة` (حالة الشيك إن كان شيكاً) — وأزرار
  `🖨️ طباعة` · `✅ ترحيل` · `💰 تحصيل` · `↩️ إرجاع` · `🗑️ إلغاء`.

## 5. التسميات العربية — نصاً أو مبرَّرة

| الديسكتوب | عندنا | |
|---|---|---|
| `📄 سند قبض عميل` · `📄 سند صرف لمورد` · `💵 قيمة السند` · `💼 تفاصيل الدفع` · `💳 نوع الدفع` · `💵 نقدي` · `🏦 تحويل بنكي` · `🔢 رقم الشيك` · `📅 تاريخ استحقاق (شيك)` · `🏦 مسحوب على بنك` · `📅 تاريخ المرجع` · `مركز التكلفة` · `👔 المندوب` · `⏰ الوقت` · `📝 ملاحظات` · `🔍 البحث` · `📅 من تاريخ` · `📅 إلى تاريخ` · `📋 كل الفترة` · `🖨️ طباعة` · `🗑️ حذف` · `💾 حفظ` · `➕ جديد` · `🏢 الفرع` · `تحت التحصيل` · `محصّل` · `مرتجع` · `📋 الحالة` · `💰 المبلغ` · `📅 التاريخ` · `🔢 الرقم` · `👤 العميل` · `🏭 المورد` · `استلمت أنا السيد`/`المستلم` | نفسها | نصاً |
| `💲 دولار` | `💲 القيمة بالعملة` + قائمة عملات | الحقل في الديسكتوب دولار واحد؛ عندنا أي عملة، والتسمية تقول ذلك |
| — | `📝 البيان` | الديسكتوب يسميه `ملاحظات` ويخزّنه في نفس العمود؛ البيان هو الاسم الذي يظهر في القيد، فجمعناهما على الاسم الأعمّ |
| — | `💰 تحصيل` · `↩️ إرجاع` | الديسكتوب يغيّر حالة الشيك بقائمة؛ شاشتنا تضع الفعل على الزر |
| — | `🏦 الصندوق` · `💳 نوع الدفع` في الشبكة | تسميات أعمدة مخترَعة لجدول نتائج البحث (الديسكتوب يعرض `Header="📄 سندات قبض"` وشبكة بلا عنوان للعمود) |

كل تسمية أخرى في الشاشة مأخوذة نصاً من الملفات المذكورة في §1.

## 6. الجزء الأول — التحقق

* `apps/api/test/treasury-vouchers.spec.ts` — **7 اختبارات**: الوثيقة تحمل البيان والوقت
  والمندوب؛ مندوب مؤسسة أخرى ووقت غير مفهوم مرفوضان؛ الترحيل يبني القيد (مدين الصندوق /
  دائن العميل) ويوازنه؛ سند الصرف يقلبه والبنك هو الدائن؛ الشيك لا يلمس الرصيد حتى
  التحصيل ويُقيَّد يومه ولا يُحصَّل مرتين؛ المرتجع يعيد الدين؛ البحث بالتاريخ والرقم
  والبيان ومؤسسة أخرى لا ترى شيئاً؛ المسوّدة تُعدَّل والمرحَّل لا يُعدَّل.
* `node scripts/verify-treasury.mjs` — **6 أقسام** تمشي الوثيقة كاملة ضد ستاك حيّ:
  إيصال ← قيد ← صرف ← شيك ← ارتجاع ← بحث، وينتهي بـ
  «✔ Phase 06 treasury documents verified».
* المجموعة الكاملة: API **82 ملفاً / 482 اختباراً**، `apps/staff` **36/36**، `tsc --noEmit`
  نظيف للتطبيقين، بناء الواجهة **100/100** صفحة.

## 7. ما ورثته المرحلة 06 من المرحلة 12 الأصلية

الوثائق موجودة أصلاً: `vouchers` (قبض/صرف)، تحويلات نقدية، أنواع المصاريف، إقفال
الوردية، أرصدة مواقع النقد، وانتقالات الشيك. ما كان ناقصاً هو **سياق الوثيقة**
(البيان، الوقت، المندوب، العملة) و**القيد نفسه** — وهو جوهر ما يفعله
`BindReceiptToEntry` في الديسكتوب.

## 8. الأجزاء القادمة (بهذا الترتيب)

| الجزء | المصادر | المحتوى |
|---|---|---|
| ~~الثاني~~ **تم** | `frmTreasury.xaml/.cs`، `frmBanks.xaml`، `ListSafeEmp.cs` | تعريف الخزن والبنوك — `📝 ملاحظات`، `👤 الموظف المسئول` (مسئولون متعددون كما في `Stock_Emps`)، `📋 حالة الصندوق`، وبطاقة البنوك كاملة. انظر §9 |
| ~~الثالث~~ **تم** | `frmRptKhzna.xaml` | كشف حركة الصندوق: `📥 وارد` · `📤 صادر` · `⚖️ الرصيد` برصيد متحرك، بترشيح التاريخ **والوقت**، مع `⚖️ الرصيد الإجمالي` و`📅 رصيد الفترة المحددة` و`رصيد سابق`. انظر §10 |
| ~~الرابع~~ **تم** | `frmCloseShift.xaml`، `frmCloseShiftDetails.xaml`، `frmCloseShiftInv.xaml` | إغلاقات اليومية: `🏦 رصيد الصندوق` · `💵 النقدي` · `🌐 الشبكة` · `🧾 الضريبة` · `📤 المصاريف` · `🏷️ الخصم` · `📉 الفرق` · `💹 الصافي`. انظر §11 |
| ~~الخامس~~ **تم** | `frmPayBank.xaml`، `frmCashCustomer.xaml` | تحويل بنكي (اختيار البنك لحظة الدفع) والعميل النقدي السريع. انظر §12 |
| ~~السادس~~ **تم** | `frmSafesTransfer.xaml`، `Reports/rptSafeTransfer.repx`، `CrystalLiteDB.txt` L1620/L1642 | مناقلة بين الخزن: إرسال / استلام / إقفال. انظر §13 |
| ~~مؤجَّل 4 و5~~ **تم** | `Class/EntryOper.cs` `BindCloseShiftToEntry`، `Form_WPF/ClosShiftAndroid.xaml.cs` L925، `EntryOper.cs` L620 | 📒 قيد الإغلاق: 📉 الفرق وحده يُرحَّل، والتحويل البنكي على حساب بنكه. انظر §14 |

## 9. الجزء الثاني — تعريف الخزن والبنوك (`frmTreasury` + `frmBanks`)

### 9.1 ما يفعله الديسكتوب فعلاً

* `frmTreasury.xaml` — `Title="تعريف الخزن"`، ثلاث مجموعات على بطاقة واحدة:
  `📋 بيانات الصناديق` (🏦 اسم الصندوق · 🏢 الفرع · 📋 حالة الصندوق · ⭐ الافتراضي)،
  و`👤 مسئولي الصندوق`، و`📝 ملاحظات`.
* `frmTreasury.xaml.cs:222` — الحفظ يشترط: «ادخل اسم الخزينة»، «يجب اختيار الفرع»،
  **«يجب اختيار موظف مسئول»**؛ ثم في **نفس المعاملة** يحذف `Stock_Emps` للصندوق ويعيد
  إدراج مسئوليه (`DELETE FROM Stock_Emps WHERE stock_id=…` ثم إدراج كل صف).
* `📋 حالة الصندوق` قيمتان: `نشط` / `مغلق` (`.xaml.cs:157`).
* `frmBanks.xaml` — `🏦 تعريف البنوك`: `🏦 اسم البنك` · `🌍 الدولة` · `🏙️ المدينة` ·
  `📍 المنطقة` · `تليفون` · `موبايل` · `💰 نسبة الاقتطاع %` · `📝 ملاحظات` ·
  `✅ تغيير في نقطة البيع`.

### 9.2 البيانات (الترحيل `0041_treasury_custody.sql`)

ترحيل جمعي مع ملف `down/`:

1. `cash_locations.notes text` — 📝 ملاحظات.
2. `cash_location_custodians (tenant_id, cash_location_id, employee_id)` — `Stock_Emps`
   بعزل مستأجر ومفتاح أجنبي حقيقي؛ فريد على `(tenant, location, employee)` حتى لا يُكرَّر
   الموظف على صندوق واحد، ومفهرس على `(tenant, employee)` لسؤال «على أي صناديق يوقّع هذا
   الموظف؟»، وRLS كبقية الجداول.
3. حقول البنك (`الدولة`، `المدينة`، `المنطقة`، `تليفون`، `موبايل`، `نسبة الاقتطاع`)
   تُحمل في كتلة `bank` JSON الموجودة أصلاً — **بلا ترحيل**، لأن الكتلة مستند مُصنَّف في
   `packages/contracts` لا مجموعة أعمدة، ولأن نصف الجدول صناديق لا معنى لتلك الحقول فيه.

### 9.3 القاعدة

| الموقف | الجواب |
|---|---|
| صندوق بلا مسئول واحد على الأقل | `422 VALIDATION_FAILED` — «يجب اختيار موظف مسئول» |
| موظف من مؤسسة أخرى، أو محذوف | `422 VALIDATION_FAILED` على `custodianIds` |
| تحديث المجموعة | استبدال كامل داخل معاملة الصندوق نفسه: حذف ثم إدراج |
| نزع آخر مسئول عن صندوق | مرفوض؛ والمسئولون **يبقون** كما كانوا (لا يُجرَّد الصندوق بالخطأ) |
| بنك بلا مسئول | مسموح — الحساب يُوقَّع بأمر تحويل لا بتوقيع أمين |
| نسبة اقتطاع خارج 0…100 | `400` من أنبوب zod على `bank.deductionPct` |

**فرق واحد عن الديسكتوب، مقصود:** الديسكتوب يحذف `Stock_Emps` أولاً ثم يُدخل، فتبقى
الصندوقة بلا مسئول لحظةً إن فشل الإدراج. عندنا الموظفون **يُتحقق منهم قبل أي كتابة**،
فخطأ مطبعي لا يمكن أن يجرّد صندوقاً من مسئوليه وهو في طريقه إلى الفشل.

### 9.4 الشاشتان

`/treasury/safes` (🏦 تعريف الخزينة) و`/treasury/banks` (🏦 تعريف البنوك) — مكوّن واحد
(`components/cash-location-master.tsx`) بتبويبات الديسكتوب الثلاث:

* `📋 بيانات الصناديق` / `📋 بيانات البنوك`: الرقم (تلقائي)، الاسم، 🏢 الفرع،
  📋 حالة الصندوق (`نشط`/`مغلق`)، ⭐ الافتراضي، 💱 العملة، 🧾 الحساب المرتبط — وللبنك
  كتلة `🏦 بيانات البنك` بكل حقول `frmBanks`.
* `👤 مسئولي الصندوق`: اختيار صندوق، ثم قائمة موظفين بصناديق اختيار.
* `📝 ملاحظات`: مربع ملاحظات للصندوق المختار.
* الشبكة: `#` · 🏦 اسم الصندوق · 🏢 الفرع · 👤 الموظف المسئول · 🧾 الحساب · 📋 حالة
  الصندوق · ⭐ · 🗑️.

**التنقل:** الخزينة صارت وحدة (`🏦`) بحد ذاتها في `navigation.ts` كما هي في الديسكتوب؛
ونُقل إليها `📄 سند قبض` و`📄 سند صرف` من المحاسبة › العمليات (المسارات لم تتغيّر ولم
تُمسّ نهاية واحدة)، وكذلك `📒 بطاقة حساب المصاريف` و`📊 إغلاق اليومية` — الشاشة تنتمي إلى
وحدة واحدة، والنسخ المكرَّرة في الشجرة هي ما أزالته المرحلة 01 أصلاً.

### 9.5 التسميات — نصاً

`🏦 تعريف الخزن` (عنوان النافذة)، `📋 بيانات الصناديق`، `👤 مسئولي الصندوق`، `📝 ملاحظات`،
`🏦 اسم الصندوق`، `🏢 الفرع`، `📋 حالة الصندوق`، `نشط`، `مغلق`، `⭐ الافتراضي`،
`🏦 تعريف البنوك`، `🏦 اسم البنك`، `🌍 الدولة`، `🏙️ المدينة`، `📍 المنطقة`، `تليفون`،
`موبايل`، `💰 نسبة الاقتطاع %`، `✅ تغيير في نقطة البيع`، `👤 الموظف المسئول`.
لا تسمية مخترَعة في هذا الجزء سوى عنوان التبويب `📋 بيانات البنوك` (الديسكتوب يسميها
`📋 بيانات البنوك` في `frmBanks` — `Header="📋 ادخل بيانات البنوك"`، فالأول مقابل لها
بنفس التركيب).

### 9.6 التحقق

* `apps/api/test/treasury-custody.spec.ts` — **6 اختبارات**: الصندوق يُحفظ بمسئوليه
  وملاحظاته ويُقرأ بهما؛ صندوق بلا مسئول مرفوض؛ موظف مؤسسة أخرى مرفوض؛ التحديث يستبدل
  المجموعة ولا يُجردها؛ بطاقة البنك كاملة ونسبة خاطئة مرفوضة؛ والقائمة تعيد المسئولين
  لكل صندوق ومؤسسة أخرى لا ترى شيئاً.
* `scripts/verify-treasury.mjs` — قسم **سابع** يمشي الجزء كله ضد ستاك حيّ (8 تحققات).

## 10. الجزء الثالث — حركة الصندوق (`frmRptKhzna`)

### 10.1 ما يفعله الديسكتوب فعلاً

`Form_WPF/frmRptKhzna.xaml` (`Title="حركة الصندوق"`) and `frmRptKhzna.xaml.cs`. Three
things decide whether this report is right, and the first is the one that is easy to get
wrong:

1. **الكشف يُقرأ من دفتر الحساب لا من جدول السندات** (`.xaml.cs` L156–L260). The screen
   resolves the safe's account — `SELECT Accounts_Index.Code FROM Accounts_Index, Stocks
   WHERE Accounts_Index.AName = Stocks.name AND Accounts_Index.acc_branch = Stocks.branch
   AND Accounts_Index.Type = 2` — and then groups `Entry_sub` by `Entry.GlobalID`. So a
   sale, a salary, a transfer and a receipt are all movements of the same safe, and a
   statement built from `vouchers` alone silently omits everything the treasury screen did
   not create.
2. **`رصيد سابق`** (L200) — when a period is chosen and ☑ `كل الفترة` is off, everything
   before `من تاريخ` is summed and prepended as a first row dated `من تاريخ − يوم`.
3. **الرصيد المتحرك والبطاقتان** — `runBalance += dept − credit` per entry;
   `⚖️ الرصيد الإجمالي` = آخر رصيد (L482)، `📅 رصيد الفترة المحددة` = آخر رصيد − الرصيد
   السابق (L502). And only `Entry.IS_Deleted = 0 AND Entry.state = 1` — a draft must not
   move a safe on paper before it moves it in the box.

شاشة الديسكتوب (`frmRptKhzna.xaml` L233–L560)، نصّاً:

| المنطقة | المحتوى |
|---|---|
| لوحة الترشيح | `🏦 حركة الصندوق` · `🏦 الصندوق` · `📅 الفترة` · ☑ `كل الفترة` · `من تاريخ:` · `من وقت (HH:mm):` (`00:00`) · `إلى تاريخ:` · `إلى وقت (HH:mm):` (`23:59`) · `🏦 عرض حركة الصندوق` |
| الشبكة | `م` · `العملية` · `الرقم` · `📅 التاريخ` · `📥 وارد` · `📤 صادر` · `⚖️ الرصيد` · `📝 البيان` |
| البطاقات | `⚖️ الرصيد الإجمالي` · `📅 رصيد الفترة المحددة` |
| الأزرار | `✖ خروج` · `📊 تصدير Excel` · `👁️ معاينة` · `🖨️ طباعة` |

وتصدير CSV برأس: `م,العملية,الرقم,التاريخ,وارد,صادر,الرصيد,البيان` (ورسالة «لا توجد بيانات
للتصدير.» إن كانت القائمة فارغة). والتقرير `Reports/RptKhzna.repx` وإعداداته من
`SettingPrint WHERE Inv_Id = 12`.

### 10.2 النهاية — `GET /cash-locations/:id/movements`

بلا ترحيل: كل شيء كان جاهزاً من الجزء الأول (`vouchers.voucher_time`) ومن المرحلة 12
(`journal_entries` / `journal_entry_lines` / `cash_locations.account_id`).

| المدخل | المعنى |
|---|---|
| `all=1` | ☑ كل الفترة — بلا نافذة، وبلا سطر `رصيد سابق` |
| `from` / `to` | `YYYY-MM-DD`؛ أي صيغة أخرى `422 MOVEMENT_DATE_INVALID` |
| `fromTime` / `toTime` | `HH:mm`، الافتراضي `00:00` / `23:59` كما في الديسكتوب؛ غير ذلك `422 MOVEMENT_TIME_INVALID` |
| `from > to` أو `fromTime > toTime` | `422 MOVEMENT_RANGE_INVALID` |

والجواب: `openingBalance`، و`rows` (أولها `isOpening: true` عند تحديد فترة)، و`totalAll`
(⚖️ الرصيد الإجمالي)، و`totalPeriod` (📅 رصيد الفترة المحددة)، واسم الصندوق وعملته.
وصندوق بلا حساب: `422 CASH_ACCOUNT_REQUIRED` — وهي رسالة الديسكتوب نفسها
«لم يتم العثور على حساب مرتبط بهذا الصندوق».

**انحراف واحد، مبرَّر:** وقت الديسكتوب في `Entry.date` نفسه (تاريخ **وساعة**)، أما عندنا
فالتاريخ على القيد والساعة على السند (`vouchers.voucher_time`). فحركة **بلا وقت مسجَّل**
لا يمكن وضعها داخل اليوم، فهي **لا تُخفى أبداً ولا تُدفع إلى الرصيد السابق** — لأن إخفاء
قيد حقيقي عن كشف حساب أسوأ من إظهاره. والحركات **ذات الوقت** تخضع للنافذة تماماً،
والأرصدة تبقى متصلة لأن كل حركة تُحصى مرة واحدة: الرصيد السابق = `date < من تاريخ` + نفس
اليوم بوقت أقدم، والصفوف = داخل النافذة (`time IS NULL OR time BETWEEN`).

### 10.3 `العملية` بالعربية

الديسكتوب يترجم رمز النوع (`Common.ResrirectionType(Entry.type)`). عندنا: السند يُسمّى
`سند قبض` أو `سند صرف` من نوعه، و`voucher_cheque` = `شيك`، و`فاتورة مبيعات` / `مرتجع
مبيعات` / `فاتورة مشتريات` / `مرتجع مشتريات` / `إذن مخزني` / `مناقلة مخزنية` /
`تسوية مخزنية` / `قيد عكسي`، وما لا مصدر له `قيد يومية`.

### 10.4 الشاشة

`/treasury/movements` — عنوانها `🏦 حركة الصندوق`، والتبويب الثالث `التقارير` أُضيف
لوحدة الخزينة (الديسكتوب يضعها في المحاسبة › تقارير محاسبية؛ ونحن نضع كشف الصندوق مع
الصندوق بعد أن صارت الخزينة وحدة قائمة بذاتها في الجزء الثاني). البطاقات الست:
`⚖️ الرصيد الإجمالي` · `📅 رصيد الفترة المحددة` · `🧾 رصيد سابق` · `📥 إجمالي الوارد` ·
`📤 إجمالي الصادر` · `🔢 عدد الحركات`. ثم لوحة الترشيح بنصّها، ثم الشبكة بأعمدة
الديسكتوب الثمانية وتذييل بالمجاميع، وزر `📊 تصدير Excel` يكتب CSV برأس الديسكتوب نفسه.

### 10.5 التحقق

* `apps/api/test/treasury-movements.spec.ts` — **6 اختبارات**: الكشف يقرأ دفتر الحساب (قيد
  يدوي + سند قبض + سند صرف، والمسوّدة غائبة)؛ الرصيد المتحرك لا يقفز؛ `رصيد سابق` يفتح
  الكشف مؤرَّخاً بيوم قبل `من تاريخ`؛ ⏰ الوقت يقصّ النهار والقيد بلا وقت لا يُخفى؛ صندوق
  بلا حساب وترشيح خاطئ؛ ومؤسسة أخرى لا ترى حركة صندوقنا.
* `scripts/verify-treasury.mjs` — قسم **ثامن** بـ13 تحققاً ضد ستاك حيّ.

## 11. الجزء الرابع — إغلاقات اليومية (`frmCloseShift`)

### 11.1 ما يفعله الديسكتوب فعلاً

`Form_WPF/frmCloseShift.xaml` عنوانه `📊 إغلاقات اليومية`، وهو **شاشة قراءة** قبل أن يكون
شاشة إقفال: شبكته تُبنى من `CasherClosed` موصولاً بـ`CasherClosed_Sub`
(`frmCloseShift.xaml.cs:214` للرئيس و`:270` للسطور)، واسم الموظف يأتي من جدول
`Employees` (`:330`)، و`LoadSelectedClose` (`:676`) يملأ التفاصيل، و`CloseShiftCustomer`
(`:735`) هو مسار الإقفال، و`frmCloseShiftDetails.xaml` يعرض 🧾 الملاحظات المعدودة،
و`frmCloseShiftInv.xaml` يعرض فواتير الوردية. الحساب نفسه لا يعيش هنا بل في
`Form_WPF/ClosShiftAndroid.xaml.cs`: L592 يجمع `📤 المصاريف` من سندات الصرف ذات
`ReceiptType = 8` داخل نافذة الوردية، وL780–L930 تنتج `SAfeNetVal` (النقدي) و`DiffVal`
(الفرق).

عمود `🔢 الرقم` في الشبكة هو `CasherClosed.ClosedID`: **الإغلاق وثيقة** يوقّعها
الموظف، لا صفًّا أنشأته قاعدة البيانات عرضاً — و`BindCloseShiftToEntry1` يبني قيداً
حول هذا الرقم، فهو ما يُصالِح عليه المحاسب.

### 11.2 البيانات (الترحيل `0042_shift_close_number.sql`)

صف `shift_closes` كان يُنشأ لحظة **فتح** الوردية ولا يحمل رقماً — ومن يسأل «أيّ إغلاق
كان الثلاثاء؟» كان جوابه مُعرّفاً. الترحيل يضيف:

```sql
ALTER TABLE shift_closes ADD COLUMN IF NOT EXISTS number text;
CREATE UNIQUE INDEX IF NOT EXISTS shift_closes_number_key
  ON shift_closes (tenant_id, number) WHERE number IS NOT NULL;
```

العمود **يقبل الفراغ قصداً**: الوردية المفتوحة مسوّدة، والرقم يُخصَّص عند الإقفال من
تسلسل المستندات (`document_sequences`، `docType: 'shift_close'`، `prefix: 'CS-'`،
`padding: 6`) تماماً كما يُرقَّم السند عند ترحيله لا عند كتابته. لا إعادة ترقيم ولا
نقل بيانات: الصفوف القديمة تبقى بلا رقم حتى تُغلق وردية جديدة.

### 11.3 القاعدة (`treasury.service.ts`)

| الدالة | السطر | ما تحسبه |
|---|---|---|
| `shiftTakings` (خاصة) | L890 | 💹 الصافي و🧾 الضريبة و🏷️ الخصم و📋 آجل من فواتير النافذة؛ 🌐 الشبكة = بطاقة + بنك − المرتجعات؛ 💵 النقدي = نقد السندات **المرحَّلة** + نقد المبيعات − نقد المرتجعات − 📤 المصاريف |
| `dayCloses` | L1075 | قائمة الإغلاقات مع 👤 الموظف و`membershipId`، بترشيح `from` · `to` · `branch_id` · `membership_id` · `user_id` |
| `shiftClose` | L1203 | إغلاق واحد بابنَيه: 🧾 الملاحظات المعدودة + سطور الملخّص |
| `closeShift` | L1256 | يخصّص الرقم، ويجمّد `summary`، ويحفظ السطور والملاحظات |

* **المسوّدة ليست نقداً.** `📤 المصاريف` و`💵 النقدي` يقرآن السندات **المرحَّلة** فقط؛
  سند غير مرحَّل وعد، ولا يُقفل درج على وعد.
* **📤 المصاريف بأربعة دلاء** (🚗 توصيل · ☕ ضيافة · 🛒 المشتريات · 🛡️ تأمين): الديسكتوب
  يأخذها من أعمدة لا تحملها فواتيرنا، فالخدمة تستنتج الدلو من **اسم نوع المصروف**
  الذي يشير إليه حساب السند — لا عمود جديد، ومؤسسة تسمّي أنواعها بالعربية تحصل على
  التقسيم مجاناً. ما لا يُعرَف يذهب إلى `other`، ولا يُخمَّن صفراً.
* **🏦 رصيد الصندوق مقابل 💵 النقدي**: الأول ما **عُدّ** فعلاً (فارغ قبل العد)، والثاني
  ما كان **متوقَّعاً** على الورق؛ `📉 الفرق` بينهما. لذلك يظهر صفراً في صف الوردية
  المفتوحة، وهذا صحيح لا نقص.
* **الإغلاق صورة ثابتة**: تُجمَّد الأرقام في `summary` لحظة الإقفال، فسند يُرحَّل بعدها
  لا يعيد كتابة ورقة أمضيت.
* **فلترة 👤 الموظف لا تُتجاهل**: الوردية مملوكة لمستخدم، والشاشة تعرض **الموظفين**؛
  فالخدمة تحوّل `membership_id` إلى مستخدميه. وإن جاء مُعرّف ليس لأحد، فالجواب **قائمة
  فارغة** لا «كل الدفاتر» — فلتر يُسقَط بصمت أسوأ من فلتر غائب.

### 11.4 النهايات

| النهاية | ما تردّه |
|---|---|
| `POST /shift-closes/open` | 201، أو **409 `SHIFT_ALREADY_OPEN`** لوردية ثانية على نفس الفرع |
| `GET /shift-closes/day-closes?from=&to=&branch_id=&membership_id=&user_id=` | الصفوف (الأموال نصوص `toFixed(4)`)؛ حتى 200 صف بترتيب `openedAt desc` |
| `POST /shift-closes/:id/close` | 201 `{id, number:"CS-000001", status:"closed", summary}` |
| `GET /shift-closes/:id` | `{shift, close, counts, lines}` — **404 `SHIFT_NOT_FOUND`** لمُعرّف مجهول أو غير uuid |

### 11.5 الشاشة (`apps/staff/app/treasury/day-close/page.tsx`)

ست بطاقات (`📊` · `💵` · `🌐` · `🧾` · `📤` · `📉`) تلخّص الفترة المعروضة، ثم بطاقة
`🏦 الوردية الحالية` بتسع فئات نقدية و`📉 الفرق` قبل الإقفال، ثم لوحة بحث
(`🏢 الفرع` · `📅 من/إلى` · `👤 الموظف` · `📆 كل الفترة` · `👁️ عرض`)، ثم جدول عناوينه
نصّ `frmCloseShift` نفسه: `🔢 الرقم` · `👤 الموظف` · `📅 إغلاق من` · `📅 إغلاق إلى` ·
`📋 آجل` · `🌐 الشبكة` · `💵 النقدي` · `💰 مجموع الشبكة والنقدي` · `📤 المصاريف` ·
`🛒 المشتريات` · `🚗 توصيل` · `🛡️ تأمين` · `☕ الضيافة` · `🏷️ الخصم` · `🧾 الضريبة` ·
`💹 الصافي` · `🏦 رصيد الصندوق` · `📉 الفرق` — مع سطر إجماليات وتفصيل موسَّع لكل صف
(`🧾 الملاحظات المعدودة` + سطور الملخّص). الرابط أُضيف إلى `apps/staff/lib/navigation.ts`
تحت `treasury.view`.

### 11.6 التسميات — نصاً

كل عنوان في الجدول والبحث والبطاقات مأخوذ نصاً من `frmCloseShift.xaml` /
`frmCloseShiftDetails.xaml` / `frmCloseShiftInv.xaml`. الاستثناءان:

| التسمية | المصدر | لماذا |
|---|---|---|
| 👁️ عرض · 📆 كل الفترة | لا يقابلها نصّ | أزرار بحث مضافة؛ الصياغة على وزن أزرار الشاشات الأخرى في المرحلة |
| 🧾 الملاحظات المعدودة | `frmCloseShiftDetails.xaml` يعرض الفئات بلا عنوان جامع | وصفٌ لمحتوى التفصيل لا تسمية مخترَعة لعمل |

### 11.7 التحقق

* `apps/api/test/treasury-dayclose.spec.ts` — **8 اختبارات**: رفض الوردية الثانية 409؛
  ترقيم `CS-` من تسلسل المستندات؛ `safeBalance` · `expected` · `diff` وتجميد الإغلاق؛
  مصروفان 20+10 يخرجان في 🚗/☕ مع نقد 300−30؛ البطاقة تُحسب في 🌐 لا في النقد؛
  التفصيل ومُعرّف غير uuid؛ عزل بين مؤسستين؛ وفلترة 👤 الموظف و📅 الفترة (لا تُتجاهل).
* `scripts/verify-treasury.mjs` — قسم **تاسع** بـ23 تحققاً ضد ستاك حيّ، أولها إقفال أيّ
  درج مفتوح من تشغيل سابق حتى يعود السكربت قابلاً للتكرار.

## 12. الجزء الخامس — التحويل البنكي والعميل النقدي (`frmPayBank` + `frmCashCustomer`)

### 12.1 ما يفعله الديسكتوب فعلاً

`Form_WPF/frmPayBank.xaml` عنوانه `🏦 اختر طريقة الدفع (تحويل بنكي)`، وهو **نافذة
اختيار لا نموذج**: لوح `🏦 البنوك المتاحة` يُبنى في `LoadBanks()` من
`SELECT id, name FROM Banks WHERE IS_Deleted=0 AND id != 1 AND id != 2`، وتحتها
`✔ موافق` / `✖ خروج`. الجملتان اللتان تحملان التصميم كله:

* `SelectedBankId == 0` → «يرجى اختيار بنك أولًا» — **التحويل لا يُسجَّل بلا بنك مسمّى**؛
* لا صفوف → «لا توجد بنوك متاحة».

والسبب أن الجواب يقرّر حساباً، لا اسماً: `frmPOSBill.xaml.cs` `BtnBank_Click` يضع
`Invoic.Bank = bankFrm.SelectedBankId` و`_payment.PayType = 2`، ثم `Class/EntryOper.cs`:

* **L493** `if (detail.PayType == 2 && detail.BankId > 2)` — التحويل المسمّى يخرج من
  دلوالشبكة العامة ويُجمع في قائمة بنكية؛
* **L537** المرتجع يُنقص من **البنك نفسه**؛
* **L620** تُجمَّع القائمة بالبنك، ويُدخل كل بنك على **حسابه** (`bank.AccCode`) لا على حساب
  الشبكة `1221001`.

الرقمان 1 و2 هما placeholder الصندوق والمحفظة في الديسكتوب؛ في السحابة البنك صفّ
`cash_locations` من نوع `bank`، فلا وجود لهذين الصفّين أصلاً.

أمّا `Form_WPF/frmCashCustomer.xaml` (`👤 عميل نقدي` — «إضافة أو اختيار عميل نقدي
للفاتورة») فقراره الحاسم في الكود الخلفي: `SearchCustomers` **لا يفتح جدول عملاء**، بل
يقرأ الفواتير نفسها:

```sql
SELECT CashCustomerName, CashCustomerMobile FROM inv
 WHERE (CashCustomerMobile = @Mobile) AND CashCustomerName <> ''
   -- أو --
 WHERE CashCustomerName LIKE '%' + @Name + '%' AND CashCustomerName <> ''
```

أي أن العميل النقدي **اسم وجوال مكتوبان على الفاتورة**، ولذلك يستطيع الكاشير إنشاء واحد
بلا فتح حساب ذمم، ولذلك «ابحث عن العميل» تعني «ابحث عن اسم خدمه المتجر قبل الآن».
و`BtnInsert_Click` يرفض الاسم الفارغ والجوال الفارغ: «يجب إدخال الاسم» و«يجب إدخال رقم
الجوال».

### 12.2 البيانات — بلا ترحيل، وهذا مقصود

* **البنك**: `invoice_payments.cash_location_id` و`vouchers.cash_location_id` يحملان
  البنك منذ المرحلة 04؛ ما كان غائباً هو **القاعدة** التي تقرأه، لا العمود.
* **العميل النقدي**: `sales_invoices.cash_customer_name` و`cash_customer_mobile`
  موجودان في المخطط وفي `POST /pos/checkout` منذ المرحلة 04 وما قبلها.

لذلك الجزء الخامس **بلا ترحيل**: الترحيل وسيلة لا غاية، وإضافة عمود لشيء موجود تعريض
بيانات للضياع بلا سبب.

### 12.3 القاعدة

**أ. التحويل البنكي (`treasury.service.ts`)**

| الموضع | ما صار |
|---|---|
| `shiftTakings` | كل دفعة `method: 'bank'` تُجمَّع ببنكها (`EntryOper.cs` L620). تحويل بلا بنك مسمّى يبقى في 🌐 الشبكة — نظير `BankId > 2` |
| `closeShift` | سطر `bank-transfer` لكل بنك في `shift_close_lines`، يحمل البنك في `metadata` لا في `party_id` (ذلك مفتاح أجنبي إلى `parties`، والبنك ليس طرفاً) |
| `dayCloses` | كل صف يردّ `banks[]` — حيّة للوردية المفتوحة، ومجمَّدة من `summary` للمُقفلة |

قرار مقصود: **🌐 الشبكة يبقى يحمل المبلغ كاملاً**، و`banks[]` تفصيل منه لا إضافة عليه،
حتى يبقى `💰 مجموع الشبكة والنقدي` مجموعاً صحيحاً. الديسكتوب يفصل البنوك في `madaTotal`
لأجل **القيد**، أما عمود الشبكة في الشبكة فيبقى مجموعاً يجمع المال كلّه.

**ب. العميل النقدي (`sales.service.ts`)**

`cashCustomers(tenantId, {name?, mobile?})` تقرأ `sales_invoices` وتجمّع بالاسم والجوال:
`mobile` تطابق تام (كما في `= @Mobile`)، و`name` تطابق جزئي (كما في `LIKE '%'+@Name+'%'`),
و`CashCustomerName <> ''` شرط في الحالتين. النهاية `GET /sales/cash-customers`.
**انحراف واحد مبرَّر:** شبكة الديسكتوب تبدأ فارغة ولا تمتلئ إلا بضغطة زر؛ وشاشة قائمة
تُفتح فارغة تبدو معطوبة، فبلا كلمة بحث نردّ آخر الأسماء خدمةً (الأحدث أولاً).

### 12.4 الشاشتان

* **🏦 اختر البنك** — مكوّن `apps/staff/components/bank-chooser.tsx` على النافذة نفسها
  (`🏦 اختر طريقة الدفع (تحويل بنكي)` · `🏦 البنوك المتاحة` · `✔ موافق` · `✖ خروج` ·
  «لا توجد بنوك متاحة» · «يرجى اختيار بنك أولًا»). موصول بـ:
  * `/sales/pos` عند اختيار `تحويل بنكي` — بدل قائمة منسدلة يتجاوزها الكاشير؛
  * `/treasury/vouchers` عند `🏦 تحويل بنكي` في السند.
* **👤 عميل نقدي** — مكوّن `apps/staff/components/cash-customer-picker.tsx`
  (`🏷️ الاسم:` · `📱 رقم الجوال:` · `✅ إدراج` · `🚪 خروج` · `🔍` بحث بالجوال · `🔍` بحث
  بالاسم · شبكة `👤 الاسم` · `📱 الجوال` · `اختيار` بزر `✔ اختار`)، وشاشة قائمة
  `/sales/cash-customers` باللوح نفسه. اختيار الاسم في نقطة البيع يملأ الاسم والجوال،
  فيُكتبان على الفاتورة.

### 12.5 التسميات — نصاً

كل تسمية أعلاه مأخوذة نصاً من الملفين. الاستثناء الوحيد: عمود **«فاتورة»** في شبكة
الشاشة المستقلّة (عدد الفواتير المجمَّعة) — لا يقابله نصّ في `frmCashCustomer`، وهو وصف
للرقم المجمَّع لا تسمية لوظيفة مخترَعة.

### 12.6 التحقق

* `apps/api/test/treasury-bank-transfer.spec.ts` — **8 اختبارات**: التحويل يسمّي بنكه لا
  الافتراضي؛ بنك لكل سطر مع بقاء 🌐 الشبكة كاملاً؛ تجميع التحويلات إلى بنك واحد؛ الإقفال
  يحفظ سطراً لكل بنك ويتجمّد به؛ بنك فرع آخر مرفوض `422 POS_CASH_LOCATION_INVALID`؛
  تحويل بلا بنك واضح لا يُقبض على حساب مُخمَّن؛ عزل بين مؤسستين؛ والنقدي لا يختلط
  بالبنوك.
* `apps/api/test/sales-cash-customer.spec.ts` — **7 اختبارات**: الاسم والجوال على
  الفاتورة؛ البحث بالجوال تطابق تام؛ البحث بالاسم في وسطه؛ تجميع الزيارات؛ الاسم الفارغ
  وصاحب الحساب ليسا عميلاً نقدياً؛ بلا بحث يردّ آخر الأسماء؛ وعزل بين مؤسستين.
* `scripts/verify-treasury.mjs` — قسم **عاشر** بـ13 تحققاً ضد ستاك حيّ، يمشي المسار
  كاملاً: بنكان، تحويلات، إقفال، سطور بنكية، ثم بحث العميل النقدي.

## 13. الجزء السادس — مناقلة الخزن (`frmSafesTransfer` + `Reports/rptSafeTransfer.repx`)

### 13.1 ما يفعله الديسكتوب فعلاً — وما لا يفعله

النافذة `Form_WPF/frmSafesTransfer.xaml` عنوانها `مناقلة`، وثلاثة تبويبات تحمل العمل
كله:

| التبويب | محتواه |
|---|---|
| 📦 التحويل | «📋 إدخال بيانات التحويل» — `🔢 رقم المناقلة` · `📅 التاريخ` · `🏪 من مخزن` · `🏪 إلى مخزن` · `✅ اعتماد الإرسال` |
| 📥 استلام تحويل | `🔢 رقم المناقلة` · `🏪 من مخزن` · `🏪 إلى مخزن` · `👁️ عرض` · `✅ تأكيد الاستلام` · `📦 استلام الكل` |
| 🔍 البحث | «🔍 معايير البحث» — `🔢 رقم التحويل` · `📅 من تاريخ` · `📅 إلى تاريخ` · `📋 كل الفترة` · `🔍 بحث`، والنتائج في «📊 نتائج البحث» |

والكود الخلفي `.xaml.cs` آلة حالات بثلاث حركات (L690 إدخال، L863 إرسال، L872 استلام):

```sql
INSERT INTO SafesTransfer (date, safe_from, safe_to, branch, IsSent, IsReceived, IS_Deleted) …
UPDATE SafesTransfer SET IsSent      = 1 WHERE …   -- «المناقلة تم إرسالها سابقاً»
UPDATE SafesTransfer SET IsReceived  = 1 WHERE …   -- «لا يمكن استلام كمية أكبر من الكمية المرسلة»
```

**ولكن** — وهذا أهم ما في الجزء — جدول `SafesTransfer` في `CrystalLiteDB.txt` (L1620)
**لا يحتوي عمود مبلغ**: أعمدته `id, date, safe_from, safe_to, branch, IsReceived,
IsSent, IS_Deleted`، والجدول الفرعي `SafesTransfer_Sub` (L1642) يحمل **الأصناف**
(`ItemId`, `value` = الكمية, `unitPercnt`, `AvrgCost`, `ReceivedValue`, `Diff`,
`IsReceived`). وحقول التقرير `Reports/rptSafeTransfer.repx` تؤكّده: الصنف · الفئة ·
المستودع · الباركود · الكمية · إجمالي الكميات.

أي أن `frmSafesTransfer` **مناقلة أصناف بين المخازن**، وأن حركة **المال** بين الخزن في
الديسكتوب تُعمَل بسند صرف + سند قبض لا بهذه النافذة. لذلك صار هذا الجزء شيئين:

1. **مناقلة النقد** — شاشة جديدة `/treasury/transfers` على `/cash-transfers`، بتبويبات
   النافذة الثلاثة وتسمياتها نصاً، وبآلة حالاتها نفسها: مسودة ← إرسال (`IsSent`) ←
   استلام (`IsReceived`). المال في «الطريق» بين الخزنتين لا يُحسب في أيٍّ منهما، وما
   أُرسل لا يُحذف بل تُرَدّ مناقلة معاكسة.
2. **مناقلة الأصناف** — الشاشة القائمة `/inventory/transfers` (وهي نظير النافذة نفسها
   للمخزون) أخذت تبويب **البحث** الذي كان ناقصاً: `🔢 رقم التحويل` و`📅 من تاريخ` /
   `📅 إلى تاريخ`.

### 13.2 البيانات — بلا ترحيل، مرّة أخرى

جدول `cash_transfers` موجود منذ `0011_treasury.sql` (L7) ومعه القيد الذي يحكم هذا الجزء:

```sql
status text NOT NULL DEFAULT 'draft'
  CHECK (status IN ('draft','sent','received','voided'))
```

الحالة الرابعة **`voided`** موجودة أصلاً في المخطط، فلا حاجة إلى ترحيل: 🗑️ حذف في
الديسكتوب هو `IS_Deleted` على الصفّ، ونظيره هنا `voided` — حالة يسمح بها المخطط بلفظه.
(محاولة كتابة `cancelled` ترفضها قاعدة البيانات بقيد التحقق، وهذا سبب اختيار `voided`.)

### 13.3 القاعدة

**أ. النقد (`treasury.service.ts` / `treasury.controller.ts`)**

| الموضع | ما صار |
|---|---|
| `transfers(tenantId, {status, number, from, to})` | يردّ `fromName` / `toName` من ضمّ `cash_locations` مرّتين باسمين مستعارين (`from_side` / `to_side`) — لا يرسل معرّفات ليترجمها المتصفح؛ `number` بـ `ilike`؛ نافذة التاريخ `T00:00:00.000Z`…`T23:59:59.999Z`؛ ترتيب `createdAt DESC` وسقف 200 صفّاً |
| `cancelTransfer` | مسودة فقط، وإلا `422 CASH_TRANSFER_INVALID_STATE`؛ الحالة المكتوبة `voided` |
| `GET /cash-transfers?status=&number=&from=&to=` | 🔍 البحث كما في التبويب الثالث |
| `POST /cash-transfers/:id/cancel` | 🗑️ حذف |

`send` و`receive` لم يتغيّرا: الإرسال يرقّم المناقلة (`CT-…`) وينقص خزنة المصدر،
والاستلام يزيد خزنة الوصول، وكلاهما داخل معاملة واحدة مع القيد.

**ب. الأصناف (`inventory.service.ts` / `inventory.controller.ts`)**

`listTransfers(tenantId, status, {number, from, to})` بالمرشّحات الثلاثة نفسها
(`gte` / `lte` على `createdAt`)، و`GET /inventory/transfers` يمرّرها.

### 13.4 الشاشة (`apps/staff/app/treasury/transfers/page.tsx`)

* التبويبات الثلاثة بنصوصها: 📦 التحويل · 📥 استلام تحويل · 🔍 البحث.
* **📦 التحويل** — «📋 إدخال بيانات التحويل»: `🏢 الفرع` · `📅 التاريخ` · `🏦 من خزنة` ·
  `🏦 إلى خزنة` · `💰 المبلغ`، ثم 💾 حفظ كمسودة؛ الخزنتان تُفلتران بفرع المناقلة.
* **📥 استلام تحويل** — `🔢 رقم المناقلة` (قائمة المناقلات المُرسَلة وحدها) تُقرأ منها
  `🏦 من خزنة` · `🏦 إلى خزنة` · `💰 المبلغ` قراءةً، ثم `👁️ عرض` (رأس وثيقة كامل)،
  `✅ تأكيد الاستلام`، و`📦 استلام الكل` بعدّاد ما في الطريق.
* **🔍 البحث** — `🔢 رقم التحويل` · `📅 من تاريخ` · `📅 إلى تاريخ` · `📋 كل الفترة` ·
  `🔍 بحث` · `📋 الحالة`، والنتائج في «📊 نتائج البحث»: `🔢 الرقم` · `📅 التاريخ` ·
  `🏦 من خزنة` · `🏦 إلى خزنة` · `💰 المبلغ` · `📋 الحالة` + إجراء الصفّ
  (`✅ اعتماد الإرسال` · `🗑️ حذف` للمسودّة، `✅ تأكيد الاستلام` للمُرسَلة).
* لوح `📊 عدد المناقلات` · `📝 مسودات` · `📤 مُرسَلة` · `📥 مُستلَمة` · `🚧 في الطريق`
  (قيمة ما أُرسل ولم يُستلم بعد).
* كل إجراء مقيّد بصلاحية `treasury.transfer.manage`، والشاشة نفسها بـ `treasury.view`.

### 13.5 التسميات — نصاً

كل تسمية أعلاه من النافذة نصاً. **استثناءان مبرَّران:**

1. `🏦 من خزنة` / `🏦 إلى خزنة` — النصّ في النافذة `🏪 من مخزن` / `🏪 إلى مخزن`: الكلمة
   «مخزن» استُبدلت بـ «خزنة» لأن الشيء المنقول هنا **نقد** والشاشة في وحدة الخزينة؛
   البنية والحرف والترتيب كما هي. الاسم الاختياري `🏢 الفرع` أُضيف لأن المناقلة تحتاج
   فرعاً لتجد ملف الترحيل، وهو شرط الديسكتوب نفسه (`branch` في جدوله).
2. `📋 الحالة` و«📊 نتائج البحث» ليستا من النافذة نصّاً (الشبكة هناك بلا عنوان)، وهما
   وصف لشبكة موجودة لا وظيفة مخترَعة؛ عنوان لوح البحث في النافذة هو «📊 نتائج البحث»
   نصّاً.

### 13.6 التحقق

* `apps/api/test/treasury-transfers.spec.ts` — **9 اختبارات**: المسودة بلا رقم حتى
  تُرسل؛ `📤 الإرسال` يرقّمها (`CT-…`) وينقص المصدر؛ `📥 الاستلام` يزيد الهدف؛ إرسال
  المُرسَلة واستلام المُستلَمة مرفوضان `422 CASH_TRANSFER_INVALID_STATE`؛ 🗑️ تُحذف
  المسودة وحدها (`voided`)؛ 🔍 البحث بالرقم وبالفترة؛ مناقلة مؤسسة أخرى لا تُرَى؛
  ومناقلة الأصناف تُبحث برقمها وبفترتها كذلك.
* `scripts/verify-treasury.mjs` — قسم **حادي عشر** بـ19 تحققاً ضد ستاك حيّ يمشي الدورة
  كاملة (مسودة → إرسال → استلام → رفض التكرار → إلغاء → بحث برقم وبفترة)، ويردّ
  `fromName` / `toName` مسمّاتين.

### 13.7 R13 — قيود التحويل: المال في الطريق له حساب (المرحلة 06 — تكملة)

**المشكلة التي كانت**: `sendTransfer`/`receiveTransfer` في `treasury.service.ts:796/822`
كانا يحرّكان `cash_location_balances` بلا قيد، وعمودا `sent_journal_entry_id` و
`received_journal_entry_id` في `cash_transfers` (الترحيل `0011`) بقيا فارغين. نقدٌ
ينتقل بين خزنتين بلا أثرٍ في دفتر اليومية — والمحاسب لا يرى أين المال بين الإرسال
والاستلام.

**لماذا يحتاج حساباً وسيطاً**: الإرسال يُخرج المبلغ من خزنة المصدر ولا يُدخله خزنة
الوصول بعد (والرصيدان يشهدان بذلك). الفجوة بينهما حقيقةٌ محاسبية، لا تُسدّ بلا حساب.
الديسكتوب لا يحتاجه لأنه ينقل المال بسندَي صرفٍ وقبض، كلٌّ منهما يسمّي الطرف الآخر
(`Form_WPF/frmPaymentVoucher.xaml.cs:559` — «السطر الأول: credit من الخزنة/البنك»)؛
والسحابة تنقله بوثيقةٍ واحدة بحالتين. ونظيره في المخزون قائمٌ منذ المرحلة 05:
`1270003` «بضاعة تحت التحويل». فهذا امتدادُ السحابة الثاني من النوع نفسه، ويُزرع تحت
`1211` «صناديق الفرع الرئيسي» الذي فيه `1211001` و`1211002` — فالورقة التالية
`1211003` «نقد تحت التحويل».

**القيود**:

| الحدث | المدين | الدائن |
|---|---|---|
| 📤 اعتماد الإرسال | `1211003` نقد تحت التحويل | حساب خزنة المصدر |
| 📥 تأكيد الاستلام | حساب خزنة الوصول | `1211003` نقد تحت التحويل |

كلاهما داخل معاملة واحدة مع حركة الرصيد، وبمفتاحٍ مُعادٍ (`cash-transfer:{id}:sent` /
`received`) يمنع التكرار. والردّ يحمل `sentJournalEntryId` / `receivedJournalEntryId`
و`cashInTransitAccountId`، والشبكة `GET /cash-transfers` تعلنهما كما تعلن الأرصدة.

**حلّ الحساب**: `cash_transfer.cashInTransitAccountId` في ملف الترحيل ← دليل
`1211003` ← `422 CASH_TRANSFER_TRANSIT_ACCOUNT_MISSING`. والخزنة نفسها بلا حساب
⇒ `422 CASH_ACCOUNT_REQUIRED` (بدل قيدٍ بسطرٍ ناقص).

**البيانات**: ترحيل `0091_cash_in_transit_account.sql` يُكمل الدليل لمستأجرٍ سُبق
تأسيسه (ومعه `ensureChartOfAccounts` يزرعه عند التأسيس التالي)، وبذرة العرض
`seed-demo.ts` تحمله (العدّاد 115⇒116).

**التحقق الموسّع**:

* `treasury-transfers.spec.ts` — **9 اختبارات** بقيت، مع تأكيد القيود: الإرسال يردّ
  `sentJournalEntryId` و`cashInTransitAccountId` والشبكة تعلنه، والاستلام يردّ
  `receivedJournalEntryId`، وكلاهما سطران متوازنان.
* `verify-treasury.mjs` — القسم 11 صار **27 تحققاً** (كان 19): +8 للقيود (وجود القيدين
  وسطراهما واتجاههما والشبكة تعلنهما).


## 14. الجزء السابع — 📒 قيد الإغلاق (`BindCloseShiftToEntry`)

### 14.1 ما يفعله الديسكتوب فعلاً

`Form_WPF/ClosShiftAndroid.xaml.cs:925` `BindCloseShiftToEntry1` (ونظيره
`Class/EntryOper.cs:404`) يبني قيداً واحداً عند كل إغلاق، ويعلّقه على رقم الإغلاق نفسه:

```csharp
entry.ReffNo = inv.ClosedId.ToString();
entry.Note   = $"اغلاق اليومية خاصة الموظف {employeeName} رقم{inv.ClosedId}";
```

وسطوره، نصّاً من الكود:

| الحساب | المدين | الدائن |
|---|---|---|
| حساب الخزنة (`treasury.AccCode`) | `netCash` (نقدي في الصندوق) | `cashSales` عند المشتريات |
| `1221001` الشبكة | `madaTotal` | — |
| حساب كل بنك مسمّى (`bank.AccCode`) | مجموع ذلك البنك (`EntryOper.cs` L620) | — |
| `4100001` المبيعات | — | `salesNet + discount` |
| `4100002` مرتجع المبيعات | `returnNet` | — |
| `2222001` الضريبة المضافة | — | `vatTotal - extraVAT` |
| `2222002` الضريبة الإنتقائية | `extraVAT` | — |
| `4100003` خصومات ممنوحة | `discount` | — |
| `4200002` إضافات · `22210001` تأمين · `12310001` مبالغ مستحقة | بحسبها | بحسبها |
| `1211002` عهدة الإغلاق | `CashierBalance` | — |
| `3110004` فرق بالصندوق | العجز / الزيادة | الزيادة / العجز |

### 14.2 لماذا لم يُنسخ هذا القيد كما هو — وهذا أهم قرار في الجزء

الديسكتوب **لا يرحّل شيئاً عند حفظ الفاتورة**؛ الإغلاق هو الحدث المحاسبي كله. أما
السحابة فتعكس ذلك تماماً: كل فاتورة مرحَّلة كتبت قيدها بنفسها (المبيعات، الضريبة،
الخصم، ومدين حساب الصندوق أو **حساب البنك باسمه** — `pos.service.ts` يردّ
`resolveTenderTarget` حساب موقع النقد المسمّى، وهو نظير `bank.AccCode` في L620).

فنسخ القيد هنا يعني ترحيل اليوم **مرّتين**. ولذلك القيد يحمل فقط ما لا يعرفه أي
مستند آخر: **📉 الفرق** — ما بين الدرج المعدود باليد والدرج الذي تتوقّعه الدفاتر:

| الحالة | المدين | الدائن |
|---|---|---|
| عجز (`counted < expected`) | فرق الصندوق | الصندوق |
| زيادة (`counted > expected`) | الصندوق | فرق الصندوق |
| مطابق | ~~**لا قيد** — `422 SHIFT_BALANCED`~~ ⇒ §R12: مدين عهدة الإغلاق / دائن الصندوق | |

قيد من سطرين صفريين ليس دليلاً، بل ضجيج في دفتر اليومية.

~~**عهدة الإغلاق (`1211002`) غير منسوخة، قصداً**: هي في الديسكتوب تركن النقد المعدود على
حساب عهدة الكاشير حتى يُودَع، ولا تملك السحابة خطوة إيداع تُقفلها — ورصيد لا شيء
يُقفله أسوأ من سطر نضيفه مع شاشة الإيداع.~~ ⇒ **§R12 نسخها**: خطوة الإيداع قائمة
(سند قبضٍ حسابُه المقابل «عهدة الإغلاق»)، والعهدة تُكتب على الإغلاق نفسه فيُبنى السند
منها لا من الرأس.

### 14.3 البيانات (الترحيلان `0043` و`0044`)

**`0043_shift_close_entry.sql`** — جمعيّ: `shift_closes.journal_entry_id` (مرجع إلى
`journal_entries`) و`posted_at`، وهما nullable لأن الصندوق المطابق بلا قيد.

**`0044_shift_close_number_scope.sql`** — **إصلاح خلل حقيقي كُشف أثناء العمل**:
`0042` جعل 🔢 الرقم فريداً على **المؤسسة** (`shift_closes_number_key`)، لكن سلسلة
الترقيم كانت على **الفرع**. فرعان يصدران `CS-000001` معاً، فيموت إغلاق الثاني
بـ**500** مفتاح مكرّر — كاشيرة في الفرع الثاني لا تستطيع إقفال درجها لأن زميلها في
الفرع الأول أقفل درجه. الإصلاح: الترقيم على المؤسسة كلها (وهو ما عليه `ClosedID` في
الديسكتوب أصلاً)، والترحيل يزرع العدّاد المؤسسي من أعلى رقم صدر فعلاً
(`MAX(regexp_replace(number,'\D','','g'))`) فلا يُعاد إصدار رقم مطبوع.

### 14.4 القاعدة

* `POST /shift-closes/:id/post` بصلاحية **`treasury.shift.post`** الجديدة — عدّ الدرج
  (`treasury.shift.close`) وترحيل قيده قراران مختلفان، كما أن التخطيط للإنتاج
  واستهلاك المخزون قراران مختلفان. مُنحت لمالك المنشأة والمحاسب ومدير الفرع وموظف
  الخزينة، **لا** للكاشير.
* الشروط بالترتيب: الوردية مُغلَقة (`422 SHIFT_INVALID_STATE`)، لم تُرحَّل بعد
  (`409 SHIFT_ALREADY_POSTED`)، وفيها ما يتحرّك: متوقّعٌ أو معدودٌ غير صفري
  (`422 SHIFT_BALANCED` للدرج الفارغ الذي لا يُنتظر منه شيء — §R12).
* مفتاح ترحيل جديد **`cashDifferenceAccountId`** (فرق بالصندوق — `3110004`) أُضيف إلى
  `POST_PROFILE_ACCOUNT_KEYS` في العقود، لأن مخطط الملف `.strict()`: مفتاح غير معروف
  يُرفض، و«حساب فرق» لا يُخمَّن.
* كل صف في `GET /shift-closes/day-closes` يردّ `journalEntryId` و`postedAt` و
  **`postable`** — حتى تعرف الشاشة متى تعرض الزر دون أن تعيد حساب الفرق.
* البيان نصّاً: `اغلاق اليومية خاصة الموظف {الاسم} رقم{CS-…}`.

### 14.5 الشاشة

`/treasury/day-close` اكتسب عمود **📒 القيد**: `✅ مُرحَّل`، أو زر `📒 ترحيل القيد`
للمُغلق غير المُرحَّل، أو `بلا قيد` لمن لا يملك الصلاحية. و~~`— مطابق` للدرج المتوافق~~
سقط في **§R12** بعد أن صار المطابق يُرحَّل، وحلّ عمود **🧾 العهدة** مكانه ليقرأ الكود
والمبلغ من الإغلاق.

### 14.6 التحقق

* `apps/api/test/treasury-shift-entry.spec.ts` — **11 اختباراً** ~~العجز والزيادة
  والتوازن، المطابق بلا قيد، الترحيل مرّتين، الوردية المفتوحة، ملف ترحيل ناقص مفتاح
  الفرق (`TREASURY_PROFILE_KEY_MISSING`)، البيان يحمل الرقم، التحويل البنكي على حساب
  البنك نفسه لا على حساب الصندوق، عدم تكرار الرقم بين فرعين، وعزل المؤسسات.~~
  ⇒ **§R12: صارت 14** (العهدة والفرق وسطرا المطابق، و422 العهدة الناقصة، والفارغ).
* `scripts/verify-treasury.mjs` — قسم **ثاني عشر** ~~بـ12 تحققاً~~ ⇒ **§R12: 119 تحققاً
  في السكربت كله** ضد ستاك حيّ يمشي المسار كاملاً (عجز → ترحيل → رفض التكرار → زيادة →
  مطابق → رقمان لفرعين → بنك)، ويفتح فوج نقطة البيع بنفسه (`feature.pos`).
* تنظيفٌ جانبيّ: `no-restricted-syntax` كان يُخفق في `sales.service.ts` وثلاثة ملفات
  اختبار (30 خطأً)، فصار `pnpm -r run lint` أخضر بالكامل.

## 15. مؤجَّل عن قصد

* ~~**قيد الإغلاق**~~ **تم** — انظر §14. القيد يحمل 📉 الفرق وحده؛ وما عداه رحّلته كل
  فاتورة عند ترحيلها، فنسخه هنا كان ترحيلاً مزدوجاً لليوم كله.
* ~~**عهدة الإغلاق** (`1211002`) — سطر عهدة الكاشير في `BindCloseShiftToEntry` لم يُنسخ:
  يحتاج خطوة إيداع تُقفله، ولا وجود لها بعد. تُضاف معها.~~ ✅ **§R12** — نُسخ سطر العهدة
  كما يُنشئه الديسكتوب (`ClosShiftAndroid.xaml.cs:1593` · `EntryOper.cs:774`)، والإيداع
  سندُ قبضٍ من `/treasury/vouchers` حسابُه المقابل «عهدة الإغلاق».
* **قيدُ التحويل بين الخزائن** (`POST /cash-transfers/{id}/send|receive`) — **لم يُغلق في
  §R12** وأُعلن بصراحة: الإرسال والاستلام يحرّكان `cash_location_balances` ولا يكتبان
  قيداً، وعمودا `cash_transfers.sent_journal_entry_id`/`received_journal_entry_id`
  (الترحيل `0011`) يبقيان فارغين. ⇒ مرشَّح البند التالي.
* **تقرير الإغلاق المطبوع** — `Reports/rptCloseShift.repx` و`rptCloseday.repx`
  و`rptClosedayCust.repx` تنتظر مرحلة التقارير؛ `printShiftData` يهيّئ بياناتها فقط.


* **إرفاق المستندات** (`📂 إرفاق` / `📎 إرفاق مستند` — `ReceiptOper.insertDocument` يكتب في
  جدول `Documents`) ينتظر مرحلة الملفات والطباعة، لا الخزينة.
* **اللون والحجم والخصائص** في `InvoiceItemDetail` تنتمي إلى القطاعات الرأسية (الملابس
  والبصريات)، لا إلى سند القبض.
* **سند صرف متعدد العملات فعلياً**: الحقل موجود (`foreign_amount`)، ولا حساب فروقات صرف
  بعد؛ تُفتح مع مرحلة العملات.

## R12. عهدة الإغلاق — قيدُ الإغلاق يصير ثلاثة أسطر، والمطابق يُرحَّل

> مراجعةُ ما بقي مفتوحاً في هذا الملف (§14 و§15) بعد إغلاق المراحل P-C وP-M وR1–R11.
> البندُ الذي كان مُعلَّقاً صراحةً: «**عهدة الإغلاق (`1211002`) غير منسوخة، قصداً**».

### R12.1 ما يفعله الديسكتوب فعلاً — وما لم نكن ننسخه

الجدول في §14.1 يذكر سطرين نسخناهما كـ«فرق فقط» ولم ننسخهما: `1211002` عهدة الإغلاق
و`3110004` فرق بالصندوق. و`Class/EntryOper.cs` يكتبهما هكذا نصّاً:

```csharp
// ─── عهدة الإغلاق ───
if (inv.CashierBalance != 0m)
{
    string balName = Common.GetAccountName(1211002);
    Account balAcc = new Account { Code = 1211002.ToString(),
        Note = $" عهدة الإغلاق {employeeName}{noteEmp}", CCcode = (-1).ToString() };
    if (inv.CashierBalance > 0m) { balAcc.Debt = inv.CashierBalance; balAcc.Credit = 0.0; }
    else { balAcc.Debt = 0.0; balAcc.Credit = FormatAmount(-1m * inv.CashierBalance); }
    list.Add(balAcc);
}

// ─── فرق الصندوق ───
if (cashSales > inv.CashierBalance)      // عجز
    diffAcc.Debt   = cashSales - inv.CashierBalance;
else if (cashSales < inv.CashierBalance) // زيادة
    diffAcc.Credit = inv.CashierBalance - cashSales;
```

وعلى المسار الأندرويدي نفسه (`Form_WPF/ClosShiftAndroid.xaml.cs:1550–1650`): كتلة
`Create Account Entries - Cash Drawer` تُدين/تدائن **حساب الخزنة** بـ`totalCashInDrawer`
، ثمّ كتلة `Create Account Entries - Cashier Balance` تركن `inv.CashierBalance` (المعدود
بيد الكاشير) على `1211002`، ثمّ كتلة الفرق. أي أنّ الديسكتوب يرحّل، في كل إغلاق، **ثلاث**
وقائع: النقد في الدرج، انتقالُه إلى عهدة الكاشير، وما اختلف عليه العدّ.

### R12.2 القرار — ثلاثة أسطر بدل سطر واحد، والمطابق يُرحَّل

السحابة تحسب المتوقّع من قيودها لا من مجموع طرق الدفع (`shiftTakings`)، لأن كل فاتورة
مرحَّلة كتبت مدينها على حساب الصندوق أصلاً. فالأسطر في السحابة:

| السطر | المدين | الدائن | المبلغ |
|---|---|---|---|
| 🧾 عهدة الإغلاق (`1211002`) | ما عُدّ بيد الكاشير | — | `countedCash` |
| 💵 الصندوق | — | ما تتوقّعه الدفاتر | `expectedCash` |
| 📉 فرق بالصندوق (`3110004`) | العجز | الزيادة | `expectedCash − countedCash` |

* **`expectedCash` لا `countedCash` في دائن الصندوق**: حساب الصندوق في السحابة استقبل
  مدين كل فاتورة نقدية، ومجموعُ مدينه هو المتوقّع؛ لو دائنّاه بالمعدود لبقي فرقٌ وهميّ
  في حساب الصندوق إلى الأبد.
* **المطابق يُرحَّل الآن**: نقدٌ موجود ولا بدّ أن يتحرّك إلى العهدة، وقيدُه سطران
  بلا فرق. أمّا المرفوض فهو الدرج الذي **لا معدودَ فيه ولا متوقَّع** (`422 SHIFT_BALANCED`)
  — قيدٌ من صفريْن ضجيجٌ لا دليل (وهو ما بقي من قرار §14.2).
* **سطرٌ لا يُكتب إذا كان صفراً**، فلا يظهر `0.0000` في دفتر اليومية، كما يفعل
  الديسكتوب (`if (inv.CashierBalance != 0m)` و`if (totalCashInDrawer != 0m)`).
* **تنظيف العهدة** يبقى على المحاسب: سند قبضٍ من `/treasury/vouchers` حسابُه المقابل
  «عهدة الإغلاق» يورّدها إلى الصندوق أو البنك. ولذلك تُكتب `custodyAccountId` و
  `custodyAmount` على الإغلاق نفسه، فيُبنى السند من الرأس لا من رقمٍ محفوظ في الشاشة.

**ما اخترعناه**: عمودُ **🧾 العهدة** في شبكة الإغلاقات تسميةٌ **منّا** (لا عمود لها في
`frmCloseShift.xaml`)؛ وحسابُ العهدة يُحسم بترتيبٍ صريح — مفتاحُ `shift_close.custodyAccountId`
في ملف الترحيل أوّلاً، ثمّ **ثابتُ الديسكتوب `1211002`** من الدليل، ثمّ **422
`SHIFT_CUSTODY_ACCOUNT_MISSING`** يُسمّي الناقص. ولا يُخترع حساب.

### R12.3 القاعدة

* `POST /shift-closes/{id}/post` — نفس الشروط بالترتيب: مغلقة (`422 SHIFT_INVALID_STATE`)،
  غير مُرحَّلة (`409 SHIFT_ALREADY_POSTED`)، وفيها ما يتحرّك (`422 SHIFT_BALANCED`).
* مفتاح ترحيل جديد **`custodyAccountId`** في `POST_PROFILE_ACCOUNT_KEYS`
  (`packages/contracts/src/organization/posting-profiles.ts`) — لأن مخطط الملف
  `.strict()`، ونظيره `cashDifferenceAccountId` أُضيف في §14.4 بالمصادفة نفسها.
* بلا مفتاح فرقٍ في الملف **وفرقٌ فعليّ** ⇒ `422 TREASURY_PROFILE_KEY_MISSING` (لا
  يزال يُقال صريحاً، فلا يُخترع حسابُ فروق).
* `summary` الإغلاق يُدمج فيه `custodyAccountId` · `custodyAmount` · `tillAmount` ·
  `differenceAmount` (تُحدَّث بـ`||` على الـJSONB فلا تُمحى بنودُ الملخّص السابقة)،
  و`POST …/post` يردّها في جسمه.
* `GET /shift-closes/day-closes` يضيف لكل صف: `custodyAccountId` · `custodyAccountCode`
  · `custodyAccountName` · `custodyAmount`، ويصير **`postable`** صادقاً للدرج المطابق
  أيضاً (`diff ≠ 0` **أو** `counted ≠ 0` **أو** `expected ≠ 0`).
* **بيانات سطور القيد** بالعربية كما في الديسكتوب: «عهدة الإغلاق {الموظف}» ·
  «نقدي في الصندوق {الموظف}» · «فرق بالصندوق {الموظف}» (`EntryOper.cs:762/776/810`).
* **البيان العام لم يتغيّر**: `اغلاق اليومية خاصة الموظف {الاسم} رقم{CS-…}` — الديسكتوب
  يبنيه في `BindCloseShiftToEntry` ويعلّق القيد على رقم الإغلاق، وهو ما تحقّقه §14.6.
* **والبذرة التجريبية تفتح العهدة**: `DEMO_POSTING_PROFILE` أضاف `custodyAccountId`
  (`1211002`) و`cashDifferenceAccountId` (`3110004`) — قبله كان إغلاقُ ورديةٍ بفرقٍ في
  نسخةٍ تجريبية يُرفض لملفٍّ ناقص وهو يملك الحسابين في دليله.

### R12.4 الشاشة

`/treasury/day-close` (`apps/staff/app/treasury/day-close/page.tsx`) اكتسب عمود
**🧾 العهدة**: للمُرحَّل يقرأ الكود والمبلغ من الإغلاق (`1211002 · 100.00`)، ولغيره
`—`. ونصّ النجاح بعد الترحيل صار يسمّي ما انتقل: «… العهدة 100.00 إلى 1211002 والفرق
0.00». ولا زرّ جديد: الترحيل كان قائماً، والذي تغيّر هو ما يكتبه.

### R12.5 التحقق

* `apps/api/test/treasury-shift-entry.spec.ts` — **14/14** (كان 11): يثبت
  `SHIFT_CUSTODY_ACCOUNT_MISSING` قبل أن يوجد `1211002`، ثمّ أنّ ملفَّ الترحيل يسبق
  الثابت (فرعٌ يسمّي «عهدة الموظفين» فيُقيَّد عليها لا على `1211002`)، ثمّ العجز
  (مدين عهدة 50 + مدين فروقات 50 / دائن صندوق 100)، والزيادة (مدين عهدة 150 / دائن
  صندوق 100 + دائن فروقات 50)، والمطابق (سطران، ولا سطر فروقات)، والدرج الفارغ
  (`422 SHIFT_BALANCED`)، و`custodyAccount*` في جسم الترحيل وفي شبكة الإغلاقات.
* `scripts/verify-treasury.mjs` — **120 تحققاً حيّاً بلا فشل** (كان 112: 107 خضراء
  و**5 حمراء** — أربعةٌ منها كانت تُثبّت السلوك القديم بعينه (فرقٌ وحده، ومطابقٌ بلا
  قيد) فصار يُثبّت الثلاثة، وواحدٌ توقّعٌ قديمٌ معطوب صُوِّب). والجديد يقرأ العهدة من
  الدليل، ويثبت الأسطر الثلاثة وبياناتها العربية وحقولَ `custody*` على الرأس وشبكة
  الإغلاقات. وعولج في السكربت عيبان ظهرا أثناء التحقق: (١) كان يبيع على الدرج وفوجُ نقطة
  البيع مغلقٌ افتراضاً (`feature.pos = false`, `packages/config/src/tenant-settings.ts:139`)
  فصار يفتحه بنفسه كما `verify-pos.mjs:87`؛ (٢) كان يتوقّع `404` لمعرّفٍ غير UUID فردّه
  النابض `400 INVALID_ID` — والتوقّع صُوِّب.
* عدّاد البذرة والتوافق: `DEMO_POSTING_PROFILE` ينمو بمفتاحين، و`.strict()` يبقى صارماً.

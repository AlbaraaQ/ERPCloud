# Desktop parity programme — من الدسكتوب إلى السحابة

The desktop product (`Desktop_ERP/`, SmartAuditERP — WPF + SQL Server, ~39K lines of
business logic in `Class/`, 273 screens in `Form_WPF/`, 95 DevExpress `.repx` reports)
is the functional specification. The cloud (this repo) is a clean-room rebuild:
**same behaviours and Arabic data, new architecture** (NestJS API + Next.js surfaces +
Postgres, multi-tenant, RBAC).

## Rules of the programme

1. Desktop code is **read for rules, never copied for architecture** — no `SqlClient`,
   no `MessageBox` in services, no static globals. Every ported rule lands behind a
   real API endpoint with tests.
2. Arabic names/codes stay verbatim (chart of accounts, invoice-type labels, units).
3. No mock UIs: a screen ships only when its endpoint exists; otherwise `status: 'api'`
   in `apps/staff/lib/navigation.ts`.
4. API compatibility is kept — porting adds endpoints/fields, never renames existing ones.
5. Each phase ends with: code + specs green + this README's status table updated.

## Status

| Phase | Scope | Status | Doc |
|---|---|---|---|
| 00 | Survey: architecture map, invoice matrix, file index | ✅ done | `PHASE_00_SURVEY.md` |
| 01 | Staff users/permissions UI + navigation dedup | ✅ done | `PHASE_01_USERS_NAV.md` |
| 02 | Sales invoice engine (calc, save, post, returns, notes) | ✅ done | `PHASE_02_SALES_ENGINE.md` |
| 03 | Purchase engine (invoices, returns, landed cost) | ✅ done | `PHASE_03_PURCHASE_ENGINE.md` |
| 04 | POS checkout + cashier shifts + day close | ✅ done | `PHASE_04_POS_SHIFTS.md` |
| 05 | Inventory (vouchers, transfers, counts, **serial/lot lifecycle**, **الرقم التسلسلي على سطر المستند**, reorder, multi-unit, barcodes, expiry, in-transit, item card, BOM/مكوّنات الصنف, production header, inventory reports) | ✅ parts 1–7 done (deferred: the batch half of the document line, §13) | `PHASE_05_INVENTORY.md` §10–§12 · `NEXT_SESSION_PROMPT_AFTER_PHASE_06.md` |
| 06 | Treasury (سند القبض والصرف بوثيقة كاملة + القيد، **تعريف الخزن والبنوك**، حركة الصندوق، **إغلاقات اليومية**، **التحويل البنكي والعميل النقدي**، **مناقلة الخزن**، **قيد الإغلاق** المؤجَّل من الجزأين 4 و5) | 🟡 parts 1–6 + the deferred close entry done (`سند القبض وسند الصرف`، `تعريف الخزن والبنوك`، `حركة الصندوق`، `إغلاقات اليومية`، `التحويل البنكي والعميل النقدي`، `مناقلة الخزن`، `قيد الإغلاق`) | `PHASE_06_TREASURY.md` §1–§15 · `NEXT_SESSION_PROMPT_AFTER_PHASE_06.md` |
| 07 | Accounting (دليل الحسابات والشجرة بالأرصدة، كشف الحساب، إنشاء قيد يومية، مراكز التكلفة، الفترات والميزان) | ✅ **done** — 📂 دليل الحسابات بالأرصدة + بطاقة الحساب (0045) · 📄 كشف الحساب بالرصيد السابق والمتحرّك وتجميعي/تفصيلي وكشف حساب رئيسي · 📒 إنشاء قيد يومية بـ⏰ الوقت و✅ قيد ضريبي والمندوب والفرق= (0046) · 🌳 مراكز التكلفة: شجرة بالأرصدة + 📊 كشف مركز الكلفة، و0047 يُصلح إبطال القيود · 🗂️ الفترات (0048: بطاقة الفترة، ➕/✏️/⚡/🔒/🔓/🗑️ بعبارات الديسكتوب) + ⚖️ ميزان المراجعة بالأعمدة العشرة + 📊 أرباح وخسائر حسابات رئيسية (23 tests + 83 live checks) | `PHASE_07_ACCOUNTING.md` §1–§8 |
| 08 | HRM (بطاقة الموظف، الإدارات والأقسام، الوظائف، الحوافز والجزاءات، دفع الرواتب، كشف حساب موظف، تقرير الرواتب) | 🟡 **part 5 done** — 👤 بطاقة الموظف + 🏢 الإدارات/الأقسام + 💼 الوظائف (0049) · 🎁 الحوافز والجزاءات (0050) · 💵 دفع الرواتب (0051) · 📄 كشف حساب موظف · 📈 حركات الموظف + 📊 تقرير الرواتب (بلا ترحيل: `GET /hrm/employee-movements` يقرأ مبيعات الموظف سطراً بسطر من `salesman_id`، و`GET /hrm/reports/salary` يقرأ إذونات الصرف بـ«كل الفترة»/الشهر والسنة؛ وتصحيحٌ لإذنٍ مرفوض كان يبقى في الدفاتر؛ 68 tests + 105 live checks) | `PHASE_08_HRM.md` §1–§8 |
| 09 | Verticals (contracting/projects, salesmen & commissions, tailoring orders, tailoring invoices, measurements, optics, marina, Salla, marina additions) | 🟢 **parts 1–9 done** — (1) 🧑‍💼 المندوبون والعمولات: بطاقة المندوب بالثلاث نسب (`comm` · `Colle_Comm` · `Profit_Comm`) + «📋 طباعة فواتير مندوب وعمولاتهم» (ترحيل 0052؛ 14 tests + 21 live checks). (2) 🧵 طلب التفصيل: `frmOrders` + `frmOrderDetails` + `frmOptions` — الطلب وحالاته الأربع وخياراته وقماشه، و⌛ المتبقي = السعر − المدفوع، و⌛ متأخّر حتى «تم التسليم» (ترحيل 0053 يضيف ستة جداول؛ `GET/POST/PATCH/DELETE /tailoring/orders` و`POST …/status` و`/tailoring/types` و`/tailoring/option-categories` و`/tailoring/option-values`؛ 11 tests + 38 live checks). (3) 🧾 فاتورة التفصيل: `frmViewOrders` + `AddNewSizes` — `Inv_Tailor` برقمها `TI-000001` وإجماليها ×1.05 وباقيها، و39 قياساً في `measurements jsonb` تُخدم بسجلّ `GET /tailoring/measurement-fields`، و«إستلام دفعة» بربطها بسند قبض (ترحيل 0054 يضيف ثلاثة جداول؛ `GET/POST/PATCH/DELETE /tailoring/invoices` و`POST …/status` و`POST …/payments` و`/tailoring/garment-types`؛ 11 tests + 41 live checks). (4) 📏 القياسات: `frmMeasurements` + `frmMeasurementDetails` + `frmMeasurementAttributes` — قياس كل عميل باسمه وتاريخه وقيمه، وبطاقة تُبنى وقت التشغيل من «خصائص القياسات» (`ISNULL(MAX(DisplayOrder),0)+1` · «🔕 تعطيل» لا حذفاً · ▲▼ مبادلة الترتيب؛ ترحيل 0055 يضيف `tailoring_measurement_attributes` وعمودي `name`/`measurement_date`؛ `GET/POST/PATCH/DELETE /tailoring/measurements` و`/tailoring/measurement-attributes` و`…/move` و`…/deactivate`؛ 11 tests + 37 live checks). (5) 👓 النظارات: `frmGlasses` + `Other_Column` — عشر قيمٍ نصية لعينين («PL» تُحفظ نصاً كما في `VarChar`)، و«⚙  أسماء الحقول» تسمّيها كل مؤسسة ويستبدلها «💾 حفظ الأسماء» (`delete` ثم `insert`)، وبدائل `isnull` في defaults الأعمدة (ترحيل 0056 يضيف `optics_field_labels`؛ `GET/POST/PATCH/DELETE /optics/prescriptions` و`GET/PUT /optics/field-labels` وقسم الطباعة بعناوين المؤسسة؛ 11 tests + 33 live checks)  (6) ⛵ المرسى — الحجوزات والمخالفات: `frmBookingM` + `frmViolationM` + `frmInvoiceRentSrch` — الحجز برقمه (BK-000001) و🔖 حالته («مؤكد»/«غير مؤكد») و🚢 نوعه («حجز عادي»/«بحر مفتوح») و⏱️ مدته (ساعة · دقيقة) و💰 قيمته، و🎁 الإضافات بعددها وسعرها، ومجاميع `CalcuAll` (`ضريبة = ROUND(الإجمالي × MainVAT ÷ 100, 2)` · الصافي = الإجمالي + الضريبة)، ورفضها الأول «يجب تحديد مدة الحجز»؛ والمخالفة ⚠️ نوعها و⏱️ مدتها (يوم) وثلاثة رفوض بترتيبها؛ و«🔍 خيارات البحث» على فواتير التأجير (ترحيل 0057 يضيف أحد عشر عموداً؛ `GET/POST/PATCH/DELETE /marina/bookings` و`/marina/violations` و`…/additions` ومرشِّحات /marina/rental-invoices؛ 11 tests + 45 live checks). (7) ⛵ المرسى — 📋 بطاقة الفئة و⏰ فترات التأجير: `frmGroupM` + `frmAddPeriod` — الفئة برقمها ورمزها واسميها وقيمتَي الساعة ونصف الساعة وعرضَيهما و🖼️ صورتها، وفترتا «ساعة» و«نصف ساعة» تُكتبان من صندوقي البطاقة في كل حفظة (`delete RentPeriodSub` ثم `insert`)، و«➕ إضافة مدة» تفتح شبكة «⏰ المدة · 💵 السعر · 🎁 العرض · 🗑️» من عشر مددٍ (`RentPeriod`)، و⏮ ◀ ▶ ⏭ تقف عند الطرف، ورفوضها «ادخل الفئة» · «الفئة تم ادخالها مسبقا» · «يجب إستكمال البيانات ⚠️» · «هذه الفئة لها ارتباطات فرعية لايمكن حذفها» · «اختر الفئة ليتم حذفها» (ترحيل 0058 يضيف سبعة أعمدة وثلاثة، ويُوسِّع قيد `period_kind`؛ `GET/POST/PATCH/DELETE /marina/groups` و`PUT …/periods` و`GET /marina/groups/navigate` و`GET /marina/rent-periods` و`DELETE /marina/vessels/{id}`؛ 9 tests + 58 live checks). (8) 🛒 متجر سلة: `FrmSallah` + `SallaAPI` + `ProductsManager` + `OrdersManager` + `CustomersManager` + `SallaAuth` — النافذة أربعة أزرار تُحصي ما تجلبه ولا تحفظه، والرمز مموضع في الكود، وقوائم «متجر سلة» الثلاث معالجاتُها فارغة؛ فصار للإحصاء مكان: `salla_products` و`salla_orders` (الرقم البعيد يمنع تكرار الفاتورة)، و«➕ إضافة منتج» يُرسل صنفاً حقيقياً بالمفاتيح الأربعة، و`PUT orders/{id}/status` بـ`{ status }`، و«👥 العملاء» قراءةٌ بلا تخزين، والنقل (`SallaTransport`) محقون — متجرٌ في الذاكرة حين يبدأ معرّف المتجر بـ`MOCK-` (ترحيل 0059 يضيف جدولين؛ `POST products/pull` · `POST products/push` · `PUT/DELETE products/{remoteId}` · `GET catalog` · `GET customers` · `POST orders/pull` · `PUT orders/{id}/status` · `DELETE orders/{id}` · `DELETE connections/{id}`؛ 10 tests + 40 live checks). (9) ⛵ المرسى — ➕ الإضافات: `frmAdditions` («📋 إضافات») و«🎁 الإضافات» من `frmBookingM` — أصغر نوافذ المرحلة: 🔢 الرقم (مقروء فقط) · 📝 الاسم · 💰 القيمة، و«يجب إدخال اسم الإضافة ⚠️» و«يجب تحديد الإضافة المراد حذفها ⚠️» وقيمةٌ فارغةٌ صفر؛ وهي التي تملأ قائمة «🎁 الإضافات» في الحجز، فاختيارٌ منها يكتب «السعر» من `SalePrice` و«الإجمالي» = الكمية × السعر، وإضافةٌ مكرَّرة تُجمَع كمّيتها على صفّها (`Quantity += quant`) و«يجب إدخال الكمية  » بكميةٍ فارغة (ترحيل 0060 يضيف `marina_additions` وعمود `addition_id` على `marina_booking_additions`؛ `GET/POST/PATCH/DELETE /marina/additions` و`GET /marina/additions/next`؛ 8 tests + 44 live checks) | `PHASE_09_VERTICALS.md` §1–§12 |
| 10 | Reports (94 `.repx` → محرّك التقارير) | 🟡 **parts 1–8 done** — (1) 📊 حركة المبيعات: `frmRptSalesInPeriod` + `RptSalesInPeriod1/2.repx` — «📊 إجمالي المبيعات» (رقم الصنف · الصنف · الكمية · الإجمالي) و«🧾 عرض الفواتير» (رقم الحركة · رقم الفاتورة · نوع الفاتورة · التاريخ · الوقت · آجل · نقدي · شبكة · الإجمالي · الضريبة · الخصم · الصافي)، و«💰 إجمالي المبيعات» تحت كل شبكة، و⏰ الوقت (HH:mm:ss) بجانب كل تاريخ، و🧾 نوع الفاتورة (نقطة البيع / عادية)، و🖨️ طباعة برأس المنشأة و«المستخدم» و«أعده · راجعه · المدير» و«لا توجد عمليات بالجدول» (10 tests + 59 live checks). (2) 📦 تقارير الأصناف: `frmRptItemsSalesDetails` (+`OperType=2` مشتريات) · `frmRptItemsSalesDetailsPOS` · `frmRptItemsProfit` · `frmRptItemsProfitDetails` · `frmRptSalesByCategory` · `frmRptCategorySaleByDay` — سبعة تقارير (مبيعات · نقطة البيع · أرباح تجميعي · أرباح تفصيلي · حسب المجموعة · اليومية للمجموعة · مشتريات) و💰 بطاقاتُ مجاميعَ متعدّدة (إجمالي صافي البيع · إجمالي الكميات · إجمالي الربح · إجمالي التكلفة · …) و«لا حركة → تجاهل» (14 tests + 126 live checks)  (3) 🧾 تقارير الفواتير والإشعارات والحركة اليومية: `frmRptInvSalesDetails` · `frmRptInvSalesDetailsPos` · `frmRptInvNotfic` · `frmRptInvPurchaseDetails` · `frmRptDailySales` · `frmRptDailyProcess` · `frmRptInvAnalysis` — سبعة تقارير بصفّ فاتورةٍ واحد تتقاسمه ثلاث نوافذ، و«📊 ملخص النتائج» عشر بطاقات يخصم منها المرتجع والإشعار الدائن، و«🔗 رقم المرجع» من رابط الفاتورة، وست حركات في «الحركة اليومية»، وثمانية أبعاد في «تحليل المبيعات» (15 tests + 159 live checks). (6) 💰 تقارير الخزينة والرواتب والمستخدمين: `frmRptKhzna` · `frmRptSalary` · `frmRptReseved` · `frmrptUsersRecords` · `frmRptRentInvoices` — خمسة تقارير: «حركة الصندوق» كشفاً بسطر «رصيد سابق» ورصيدٍ متحرك والبطاقتين، و«تقرير الرواتب» بإذنٍ لكل سطر (الإجمالي = الصافي + الخصومات)، و«تقرير الرواتب المستحقة» بمسيّرٍ لكل سطر وعدّاد موظفيه ومستحقه، و«سجلات المستخدمين» من سجلّ التدقيق، و«تقرير فواتير التأجير» بتأجير/معلق/حجوزات وبطاقات «إيرادات · مرتجع · الصافي» (9 tests + 62 live checks). (5) 📒 تقارير المحاسبة: `frmRptBalances` · `frmRptEntries` · `frmRptIncomeStatement` · `frmRptCostCenter` · `frmTaxRptPeriod` — ستة تقارير: «أرصدة الحسابات» بعشرة أعمدة وصيغة الرصيد من وجهين و«الحساب الرئيسي» شجرةً، و«القيود اليومية» بستة عشر نوع قيد من `EntryTypes` (وحالة «لاغي» من القيد العكسي)، و«🧾 تفاصيل القيد» بإجمالي المدين والدائن والفرق، و«أرباح وخسائر حسابات رئيسية» بـ`FinalAcc = 2` مُجمَّعةً على الآباء مع سطر المخزون وصافي الربح، و«تقرير مراكز التكلفة» تجميعي/تفصيلي بأربعة عشر عموداً، و«الإقرار الضريبي» بثلاثة عشر بنداً من `TaxRptPeriod.repx` وصافي الضريبة و«ربع سنة»/«شهري» (12 tests + 77 live checks). (7) 🖨️ إعدادات الطباعة: `frmSettings.xaml` «خيارات الطباعة» + `frmInvRptType.xaml` «🖨️ افتراضي طباعة الفواتير» + `Class/Print.cs` + `Reports/header.repx`/`footer.repx` — جدول `print_settings` (ترحيل 0061) بسبعة نطاقات هي راديوات «🧩 تفعيل إعدادات الطباعة» (الإفتراضي · مشتريات · مبيعات · نقطة بيع · تأجير · عقود · تقارير) ونطاق `report:<key>` لكل تقرير، يبحث `report:<key>` → «تقارير» → «الإفتراضي»؛ والورقة تحترمها: «عدد النسخ» نسخاً كلٌّ في صفحة كما يكرّر `Printing()` الطباعة، ونوع الورق A4 أو 🧾 ورق صغير 80mm، والترويسة والتذييل (`header.repx`/`footer.repx`) والختم و«ملاحظات التقرير»، والطابعتان تُعرضان للمشغّل ولا تُطبعان، و`?copies=`/`?paper=` يتجاوزانها لمرةٍ واحدة، و«👁️ معاينة الطباعة» و«طباعة / PDF» ورقةٌ واحدة، والوثائق الخمس (الفاتورة · السند · القيد · الإغلاق) تقرأ نطاقها كما يقرأها الديسكتوب (`new Print(1)` · `new Print(3)` · `Inv_Id=6` · `Inv_Id=11`) (15 tests + 50 live checks). (8) 📑 كشوف الحساب — **خارج جدول `frmRpt*`**: `frmCustAccount` «أرصدة حساب العملاء» (سبعة أعمدة بـ«📌 الحالة»، وحركة العميل هي حركة حسابه، ولا يُطبع من لا حركة له) و`frmCustAccountGet` «📋 كشف حساب عميل» (قيدٌ بسطر وأربع بطاقات يوضع الرصيد فيها على جانبٍ واحد) و`frmCustLastPay` «📋 حركة آخر سداد للعملاء» (آخر قيدٍ بلا مربّع تاريخ، وثلاث بطاقات) → `customer-balances` و`party-statement` و`customer-last-payment` بفلتر «🏷️ نوع الحساب» (الكل · عملاء · موردين)؛ واستُكملت النوافذ الأربع الحيّة من المرحلتين 07 و08 (`frmAccountBalance` · `frmAccountsStatement` · `frmCostCenterBalance` · `frmEmpAccountGet`) بفلترَي ⏰ الوقت و📋 نوع القيد على `GET /statements/general-ledger/:id` و`GET /statements/cost-center/:id` (14 tests + 2 appended + 67 live checks) (4) 📚 تقارير المخزون والأرقام التسلسلية: `frmRptInventory` · `frmRptItemsActivity` · `frmRptItemsActivityDetailed` · `frmRptItemsExpiration` · `frmRptSerialNo` · `frmRptSerialNoSummary` · `frmRptProducedItems` — ثمانية تقارير: «📋 الفواتير» بثمانية أنواع مستندات مخزون، و«مادة باجمالي الحركات» بثلاثة عشر عمود حركة وصيغة الرصيد (`100+20−5−10+2−1−25+3−2+7−4−8 = 77`)، و«حركة صنف تفصيلي» برصيدٍ متحرك، و«صلاحية المواد» (`dbo.ItemsExpirationStock`) بباقي سنوات وأشهر وأيام، و«حركة/أرصدة الأرقام التسلسلية» (`funCalculateSerialNoSummary`)، و«تقرير مواد المنتجة» بمكوّناته (10 tests + 150 live checks) | `PHASE_10_REPORTS.md` §1–§11 |
| 11 | الفاتورة الإلكترونية — زاتكا · ETA · التكاملات | 🟡 **الأجزاء الأول والثاني والثالث done** — ⚙️ إعدادات الربط الضريبي: `frmZatcaSetting` (472 + 1160) و`Class/ZatcaService.cs` — جدول `einvoice_settings` (ترحيل 0062) وأعمدة الشهادة على `einvoice_credentials`؛ تسعة مسارات (`GET/PUT /einvoice/settings` · `POST …/fill-from-company` · `POST /einvoice/csr/generate` · `POST /einvoice/onboarding/{compliance-csid,production-csid,compliance-check,renew}` · `POST /einvoice/link/toggle`)؛ طلب PKCS#10 حقيقي يتحقّق منه `openssl`، وبوابةٌ بثلاثة أوضاع (🧪 محاكاة · 🔵 امتثال · 🔴 إنتاج) تُرجع 502 بعبارةٍ صريحة حين لا تُبلغ؛ والست وثائق لاختبار الربط بحالاتها (388/383/381 × 0100000/0200000) وفحصٍ محليٍّ يغلق الحساب؛ وشاشة `/settings/zatca` بتسميات النافذة (18 tests + 64 live checks). و**(2) 🧾 الإرسال والتوقيع والسلسلة**: `frmSentEinvoice` (358/304) بأعمدة `frmInvsSyncStatusZatca` (559) و`ZatcaService.IntegrateInvoice` — ترحيل المبسّطة وتخليص الضريبية والوثيقة المُصادَقة ورمزها المقروء منها، وثلاثة مسارات (`GET /einvoice/filings` · `GET …/:id` · `GET /einvoice/chain`) و🔁 إعادة الإرسال، وترحيل `0063` يضيف `chain_index` · `authority_status` · `cleared_invoice`؛ وشاشة `/settings/zatca/sent` (16 tests + 53 live checks). و**(3) 📊 حالة المزامنة**: `frmInvsSyncStatusZatca` (559/1165) تقريراً مسجّلاً في محرّك التقارير (`einvoice-sync-status`) بأعمدته الأحد عشر ومرشّحاته (🔄 حالة المزامنة · 📋 نوع الفاتورة · 📅 الفترة الزمنية) ومجاميعه الثلاثة، فصارت 🖨️ طباعة و👁️ معاينة و📊 تصدير Excel تمرّ بمحرّك الطباعة كما يمرّ `PrintReport` بـ`rptInvSumByClient.repx`؛ و`POST /einvoice/sync` هو 🔄 مزامنة ZATCA يرسل المحدَّد سطراً سطراً ويجيب `sent`/`failed`/`skipped` لكل سطر؛ وشاشة `/settings/zatca/status` (16 tests + 53 live checks). و**(5) 💳 بوابات الدفع**:
  «إعدادات جيديا» وGroupBox «NeoLeap» من `frmSettings.xaml` (L1726-L1831) مع `Geidea.cs`
  و`NeoleapService.cs` ومساري التحصيل في `frmPOSBill`/`frmPOSPay` — ترحيل `0064` يضيف
  `payment_gateway_settings` و`payment_gateway_transactions` (السجلّ الذي لم يكن عند
  الديسكتوب)، وجيديا على مواصفتها المنشورة (جلسة + سؤالها) ونيوليب على عقد `SALE`
  وأجوبته `00`/`01`/`02`؛ ستة مسارات (`GET /payment-gateways` · `PUT …/:provider` ·
  `POST …/:provider/test` · `POST …/:provider/sale` · `GET …/transactions` ·
  `POST …/transactions/:id/refresh`) وشاشة `/settings/payment-gateways` (16 tests +
  64 live checks). و**(6) 📱 واتساب**: «💬 واتساب» من `frmInvSale.xaml` (L1190) ومعالجه
  `printwhatsapp` (L3124-L3199) مع `WhatsAppSender.cs` (267) و`Session.cs` (L12-L31) —
  ترحيل `0065` يضيف `whatsapp_settings` (الرقم · الرمز المشفَّر · رمز الدولة · 📎 · 🧪)
  و`whatsapp_messages` (السجلّ الذي لم يكن عند الديسكتوب)، والنداء على Cloud API كما
  نُشرت (Graph `v21.0`: نصّ، ثم رفع ملفّ، ثم رسالة مستند، و`GET /{phone-number-id}`
  ل🧪 اختبار)، والتحية تحية الديسكتوب والرقم بقاعدته؛ خمسة مسارات (`GET/PUT
  /whatsapp/settings` · `POST /whatsapp/test` · `POST /whatsapp/send` · `GET
  /whatsapp/messages`) وشاشة `/settings/whatsapp` + بطاقة «💬 واتساب» وسجلّها على نافذة
  الفاتورة (17 tests + 71 live checks). 🇪🇬 مصر خارج النطاق بقرارٍ صريح (§11) |
  `PHASE_11_EINVOICING.md` §1–§11 |

Deferred by the owner: marketing CMS, per-tenant mobile-shop module.
Done earlier, outside this programme: RBAC reorganisation (PR #4), desktop chart
seeding (112 accounts + COGS extension, `packages/database/src/desktop-coa.ts`).
Phase 05 added two more leaves (115 accounts) and three posting-profile keys; an
existing tenant is completed in place by `OrgProvisioningService`.
